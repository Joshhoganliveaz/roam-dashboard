---
phase: 01-foundation-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - vitest.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/types.ts
  - src/lib/defaults.ts
  - src/lib/slugify.ts
  - src/lib/slugify.test.ts
  - src/lib/supabase/client.ts
  - src/lib/supabase/server.ts
  - src/lib/supabase/schema.sql
  - src/middleware.ts
  - src/app/login/page.tsx
  - src/app/api/login/route.ts
  - src/app/h/[slug]/page.tsx
  - .env.local.example
autonomous: true
requirements:
  - PROD-02
user_setup:
  - service: supabase
    why: "Database and storage for homeowner records and photos"
    env_vars:
      - name: NEXT_PUBLIC_SUPABASE_URL
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL"
      - name: NEXT_PUBLIC_SUPABASE_ANON_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> anon/public key"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard -> Project Settings -> API -> service_role key"
    dashboard_config:
      - task: "Create new Supabase project for Homeowner Journey Map"
        location: "Supabase Dashboard -> New Project"
      - task: "Run schema.sql in SQL Editor to create homeowners table"
        location: "Supabase Dashboard -> SQL Editor"
      - task: "Create 'photos' storage bucket with public access"
        location: "Supabase Dashboard -> Storage -> New Bucket"
  - service: admin-password
    why: "Password protecting admin routes"
    env_vars:
      - name: SITE_PASSWORD
        source: "Choose any password Josh will use to access /admin"

must_haves:
  truths:
    - "Next.js 16 project builds and runs with dev server"
    - "TypeScript interfaces define the homeowner data contract"
    - "Supabase clients connect to the database from both browser and server"
    - "Admin routes redirect to login when unauthenticated"
    - "Login with correct password grants access to admin routes"
    - "Slug generation produces URL-safe strings from client names"
    - "Duplicate slugs get auto-suffix (smith-family-2)"
    - "Public route /h/[slug] exists and will serve homeowner pages"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "HomeownerRecord and HomeownerInput interfaces"
      exports: ["HomeownerRecord", "HomeownerInput", "HomeownerMetrics", "AdminFormState"]
    - path: "src/lib/slugify.ts"
      provides: "Slug generation and uniqueness checking"
      exports: ["generateSlug", "ensureUniqueSlug"]
    - path: "src/lib/defaults.ts"
      provides: "Smart defaults for optional fields"
      exports: ["DEFAULTS", "applyDefaults"]
    - path: "src/lib/supabase/server.ts"
      provides: "Server-side Supabase client"
      exports: ["createClient"]
    - path: "src/middleware.ts"
      provides: "Password auth protecting /admin routes"
  key_links:
    - from: "src/middleware.ts"
      to: "/admin/*"
      via: "cookie check on request"
      pattern: "cookies.*session"
    - from: "src/lib/slugify.ts"
      to: "src/lib/supabase"
      via: "database uniqueness check"
      pattern: "supabase.*from.*homeowners.*slug"
---

<objective>
Scaffold the Homeowner Journey Map project and build all foundational infrastructure: Next.js 16 project with Tailwind 4, TypeScript interfaces, Supabase clients, password auth middleware, slug generation with tests, and route stubs.

Purpose: Every subsequent plan depends on this infrastructure -- types, database access, auth, and URL routing.
Output: A running Next.js dev server with types defined, Supabase connected, admin routes protected, slug generation tested, and route structure in place.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-admin/01-CONTEXT.md
@.planning/phases/01-foundation-admin/01-RESEARCH.md

<interfaces>
<!-- Reference patterns from existing projects -->

From ~/stranalyzer/str-analyzer/src/lib/supabase/server.ts (Supabase SSR pattern):
- createServerClient from @supabase/ssr with cookie handling via next/headers

From ~/Projects/liveaz-idea-pipeline/src/middleware.ts (Auth pattern):
- Cookie-based session check, redirect to /login if missing
- Matcher excludes _next/static, _next/image, favicon.ico

From 01-RESEARCH.md Architecture section:
- Full project structure with src/app/, src/lib/, src/components/ layout
- Route structure: /admin/*, /login, /api/login, /h/[slug]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffold, config, types, and defaults</name>
  <files>
    package.json, tsconfig.json, next.config.ts, postcss.config.mjs, vitest.config.ts,
    src/app/layout.tsx, src/app/page.tsx, src/app/globals.css,
    src/lib/types.ts, src/lib/defaults.ts, .env.local.example
  </files>
  <action>
    Create a new Next.js 16 project in ~/Projects/homeowner-journey-map/.

    1. Initialize with: `npx create-next-app@16.1.6 --typescript --tailwind --eslint --app --src-dir --no-import-alias`
       Then install additional deps per RESEARCH.md install commands (date-fns, zustand, papaparse, @supabase/supabase-js, @supabase/ssr, vitest, @vitejs/plugin-react).

    2. Configure Tailwind CSS 4 with Live AZ Co brand colors as CSS custom properties in globals.css:
       - Olive (#5C6B4F), Canyon (#8B6F5C), Gold (#C9953E), Cream (#FAF7F2), Charcoal (#2C2C2C)
       - Import Source Serif 4 and DM Sans via next/font/google in layout.tsx

    3. Create `src/lib/types.ts` with these interfaces (follow patterns from RESEARCH.md):
       - HomeownerRecord: full database record shape (matches schema.sql columns, camelCase)
       - HomeownerInput: subset used by calculation engine (purchasePrice, purchaseDate, currentValue, optional loan/rent fields)
       - HomeownerMetrics: calculation output (equity, appreciation, earnings, comparisons, scenarios, net proceeds)
       - AdminFormState: form state for Zustand store (all form fields + slug + actions)
       - PageStatus type: 'draft' | 'published' | 'archived'

    4. Create `src/lib/defaults.ts` with DEFAULTS constant and applyDefaults function per RESEARCH.md:
       - loanToValue: 0.80, interestRate: 0.065, loanTermYears: 30
       - rentYieldMonthly: 0.008/12, agentCommissionRate: 0.05, closingCostRate: 0.02
       - sp500AnnualReturn: 0.105, helocLtvMax: 0.80, appreciationRate: 0.04

    5. Create root page.tsx that redirects to /admin.

    6. Create .env.local.example with all required env vars documented.

    7. Create vitest.config.ts following Idea Pipeline pattern (Vitest 4.x with @vitejs/plugin-react).

    Note: Do NOT install or configure Zustand store yet -- that belongs in Plan 03 with the admin form.
    Note: Do NOT create Supabase clients, middleware, or auth routes yet -- that is Task 2.
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm run dev` starts without errors
    - `npm run build` completes successfully
    - src/lib/types.ts exports HomeownerRecord, HomeownerInput, HomeownerMetrics, AdminFormState
    - src/lib/defaults.ts exports DEFAULTS and applyDefaults
    - Tailwind 4 configured with brand colors
    - vitest.config.ts configured and ready for test files
    - .env.local.example documents all required env vars
  </done>
</task>

<task type="auto">
  <name>Task 2: Supabase clients, auth middleware, login flow, and route stubs</name>
  <files>
    src/lib/supabase/client.ts, src/lib/supabase/server.ts, src/lib/supabase/schema.sql,
    src/middleware.ts, src/app/login/page.tsx, src/app/api/login/route.ts,
    src/app/h/[slug]/page.tsx
  </files>
  <action>
    Build all Supabase and auth infrastructure on top of the scaffold from Task 1.

    1. Create Supabase clients (src/lib/supabase/client.ts and server.ts) following exact pattern from RESEARCH.md Pattern 3. Browser client uses createBrowserClient, server client uses createServerClient with cookie handling via next/headers.

    2. Create schema.sql with full homeowners table per RESEARCH.md schema including RLS policies, indexes, and updated_at trigger. This file is for the user to run in Supabase SQL Editor.

    3. Create middleware.ts following RESEARCH.md Pattern 2: protect /admin/* routes, allow /h/*, /login, /api/login, static assets. Check for session cookie, redirect to /login if missing.

    4. Create login page (src/app/login/page.tsx) with simple password form styled with brand colors. Form POSTs to /api/login.

    5. Create API route (src/app/api/login/route.ts) that validates against SITE_PASSWORD env var and sets httpOnly session cookie with reasonable expiry (e.g., 7 days).

    6. Create placeholder /h/[slug]/page.tsx that reads the slug param and renders "Page for {slug} - Coming in Phase 2". This is the public route structure.
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Supabase client files exist with proper SSR pattern
    - schema.sql contains homeowners table with RLS policies
    - middleware.ts protects /admin routes and allows /h/* and /login
    - Login flow works (POST to /api/login with correct password sets cookie, redirects to /admin)
    - /h/test-slug renders placeholder page
    - `npm run build` still passes with zero errors
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Slug generation with uniqueness handling and tests</name>
  <files>src/lib/slugify.ts, src/lib/slugify.test.ts</files>
  <behavior>
    - generateSlug("Smith Family") returns "smith-family"
    - generateSlug("John & Jane O'Brien") returns "john-jane-obrien"
    - generateSlug("  Spaces  Everywhere  ") returns "spaces-everywhere"
    - generateSlug("123 Main St Buyer") returns "123-main-st-buyer"
    - generateSlug("") returns "" (empty input)
    - generateSlug with unicode characters strips them cleanly
    - ensureUniqueSlug returns base slug when no conflicts exist
    - ensureUniqueSlug returns "smith-family-2" when "smith-family" exists
    - ensureUniqueSlug returns "smith-family-3" when both "smith-family" and "smith-family-2" exist
  </behavior>
  <action>
    Write tests first in src/lib/slugify.test.ts covering all behaviors above. For ensureUniqueSlug tests, mock the Supabase client to return controlled slug lists.

    Then implement src/lib/slugify.ts with:
    - generateSlug(clientName: string): string -- pure function, no async, URL-safe output
    - ensureUniqueSlug(baseSlug: string, supabase: SupabaseClient): Promise<string> -- queries homeowners table for matching slugs, appends numeric suffix if needed

    Follow the implementation pattern from RESEARCH.md Pattern 4. Use a LIKE query with `${baseSlug}%` to find all potential conflicts in one query.
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx vitest run src/lib/slugify.test.ts</automated>
  </verify>
  <done>
    - All slug generation tests pass
    - generateSlug produces clean, URL-safe slugs from various inputs
    - ensureUniqueSlug correctly appends numeric suffixes for duplicates
    - Edge cases handled: empty strings, special characters, unicode, multiple spaces
  </done>
</task>

</tasks>

<verification>
- `npm run build` passes with zero errors
- `npx vitest run` passes all slug tests
- Visiting localhost:3000 redirects to /admin which redirects to /login (auth working)
- Visiting localhost:3000/h/test renders placeholder (public route working)
</verification>

<success_criteria>
- Running Next.js 16 project with Tailwind 4 and Live AZ Co branding
- Complete TypeScript interface definitions for the entire homeowner data model
- Supabase client/server files ready for database operations
- Password auth middleware protecting admin routes
- Slug generation with tested uniqueness handling
- Vitest configured and passing
- All route stubs in place (/admin, /login, /h/[slug])
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-admin/01-01-SUMMARY.md`
</output>
