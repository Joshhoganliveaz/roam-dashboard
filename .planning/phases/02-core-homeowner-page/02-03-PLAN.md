---
phase: 02-core-homeowner-page
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/components/homeowner/AppreciationChart.tsx
  - src/components/homeowner/SP500Callout.tsx
  - src/components/homeowner/HomeownerPage.tsx
  - package.json
autonomous: false
requirements: [DATA-03, DATA-06]

must_haves:
  truths:
    - "Page includes an appreciation area chart showing home value growth from purchase to today"
    - "Chart uses olive-to-gold gradient fill with smooth interpolated curve"
    - "Chart has annotated markers at purchase point and today"
    - "S&P 500 comparison displays as a callout card below the chart, not as a second line"
    - "Chart renders correctly on mobile (375px) without horizontal overflow"
    - "Homeowners with very short ownership (under 3 months) see a graceful alternative to an empty chart"
  artifacts:
    - path: "src/components/homeowner/AppreciationChart.tsx"
      provides: "Recharts AreaChart with gradient fill and data generation"
      min_lines: 40
    - path: "src/components/homeowner/SP500Callout.tsx"
      provides: "S&P comparison callout card"
      min_lines: 15
  key_links:
    - from: "src/components/homeowner/AppreciationChart.tsx"
      to: "recharts"
      via: "AreaChart, Area, XAxis, YAxis, Tooltip, ResponsiveContainer imports"
      pattern: "from.*recharts"
    - from: "src/components/homeowner/HomeownerPage.tsx"
      to: "src/components/homeowner/AppreciationChart.tsx"
      via: "renders chart in section 7 of narrative"
      pattern: "<AppreciationChart"
---

<objective>
Install Recharts, build the appreciation chart with olive-to-gold gradient and S&P callout card, wire into the HomeownerPage, and verify the complete page visually.

Purpose: The chart is the visual centerpiece of the financial story -- it transforms abstract appreciation numbers into a tangible growth curve. The S&P callout adds competitive context. Visual verification confirms the entire Phase 2 page works end-to-end.
Output: Complete homeowner page with all 9 sections rendering real data, including the appreciation chart.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-homeowner-page/02-CONTEXT.md
@.planning/phases/02-core-homeowner-page/02-RESEARCH.md
@.planning/phases/02-core-homeowner-page/02-01-SUMMARY.md
@.planning/phases/02-core-homeowner-page/02-02-SUMMARY.md

<interfaces>
<!-- From prior plans -->

From src/lib/format.ts:
```typescript
export function formatCurrency(value: number, decimals?: number): string;
```

From src/components/homeowner/SectionWrapper.tsx:
```typescript
export function SectionWrapper({ bg, children }: { bg: "cream" | "white"; children: React.ReactNode }): JSX.Element;
```

From src/lib/types.ts (relevant subset):
```typescript
export interface HomeownerRecord {
  purchasePrice: number;
  purchaseDate: string; // ISO date "YYYY-MM-DD"
  currentValue: number;
}

export interface HomeownerMetrics {
  annualizedAppreciation: number;
  sp500Comparison: { homeReturn: number; sp500Return: number; difference: number; };
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and build appreciation chart + S&P callout</name>
  <files>package.json, src/components/homeowner/AppreciationChart.tsx, src/components/homeowner/SP500Callout.tsx, src/components/homeowner/HomeownerPage.tsx</files>
  <action>
    **Install Recharts:**
    ```bash
    cd ~/Projects/homeowner-journey-map && npm install recharts
    ```

    **AppreciationChart (`src/components/homeowner/AppreciationChart.tsx`):**
    Mark as `"use client"`. Props: `{ purchasePrice: number; currentValue: number; purchaseDate: string; annualizedAppreciation: number }`.

    1. **Data generation function** `generateChartData`:
       - Calculate total months from purchase to today using date-fns `differenceInMonths`
       - If totalMonths < 3: render a simplified "too early for a chart" message instead of the chart (graceful handling per research pitfall 5). Show current value as a standalone highlight instead.
       - For ownership >= 3 months: generate monthly data points from purchase date to today
       - Each point: `{ date: format(pointDate, "MMM yyyy"), value: Math.round(purchasePrice * Math.pow(1 + annualizedAppreciation, monthIndex / 12)) }`
       - Force last point's value to exact `currentValue` (avoid rounding drift)
       - For short ownership (3-12 months): generate biweekly points for a smoother curve

    2. **Chart rendering:**
       - Wrap in SectionWrapper with `bg="white"`
       - SB7 narrative intro: "Your home vs the market" or similar
       - `<ResponsiveContainer width="100%" height={300}>`
       - `<AreaChart>` with proper margins for mobile (left: 0, right: 10)
       - `<defs>` with `<linearGradient id="appreciationGradient">`: top stop olive (#5C6B4F, opacity 0.8), bottom stop gold (#C9953E, opacity 0.1)
       - `<XAxis dataKey="date">` with tick fontSize 12, show every Nth label to avoid crowding on mobile (calculate interval based on data points count)
       - `<YAxis>` with `tickFormatter={(v) => "$" + (v/1000).toFixed(0) + "k"}`, width 55, tick fontSize 12
       - `<Tooltip>` with `formatter` using formatCurrency
       - `<Area type="monotone" dataKey="value" stroke="#5C6B4F" strokeWidth={2} fillOpacity={1} fill="url(#appreciationGradient)" />`
       - Per locked decision: annotated markers at purchase point ("Purchased: $X") and today ("Today: $X"). Use Recharts `<ReferenceDot>` for both points with custom labels.

    **SP500Callout (`src/components/homeowner/SP500Callout.tsx`):**
    - Props: `{ sp500Comparison: HomeownerMetrics["sp500Comparison"] }`
    - Rendered as a callout card below the chart (NOT inside AreaChart, NOT as a second line)
    - Card styling: rounded border, subtle olive border/shadow, padding, centered text
    - Content format per locked decision: "Your home gained $X vs $Y if you'd put the same money in the S&P 500"
    - Use formatCurrency for both values
    - If home outperformed: use gold/olive accents. If underperformed: still show honestly, neutral tone

    **Update HomeownerPage:**
    - Replace the chart placeholder comment with:
      - AppreciationChart component (passing record.purchasePrice, record.currentValue, record.purchaseDate, metrics.annualizedAppreciation)
      - SP500Callout component below it (passing metrics.sp500Comparison)
    - The chart section sits between EarningsReframe and RentVsOwn in the narrative order
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Recharts installed. AppreciationChart renders area chart with olive-to-gold gradient, annotated purchase/today markers, and responsive mobile sizing. SP500Callout renders as separate card below chart. HomeownerPage renders all 9 sections in correct order. Build passes with no errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete homeowner page</name>
  <files>none</files>
  <action>
    User visually verifies the complete homeowner page at /h/[slug] with all 9 narrative sections: hero banner, personal note, purchase story, current value reveal, equity gained, earnings per week, appreciation chart with S&P callout, rent vs own comparison, and net proceeds with expandable breakdown. Mobile-first responsive design with Live AZ Co branding.

    Steps to verify:
    1. Start the dev server: `cd ~/Projects/homeowner-journey-map && npm run dev`
    2. Create a test homeowner record via the admin form at http://localhost:3000/admin/new (set status to "published")
    3. Visit http://localhost:3000/h/[slug] with the created slug
    4. Verify the full page renders with all 9 sections in order
    5. Check the hero section (photo or gradient fallback, name, address, Live AZ Co branding)
    6. Scroll through each data section -- confirm SB7 pattern (narrative intro before numbers)
    7. Verify the appreciation chart renders with gradient fill
    8. Verify S&P callout appears as a card below the chart
    9. Verify rent vs own shows side-by-side contrast
    10. Click "See the breakdown" on net proceeds -- confirm expandable toggle works
    11. Test on mobile viewport (375px width in browser dev tools)
    12. Visit a nonexistent slug -- confirm 404 page renders
  </action>
  <verify>User confirms visual quality by typing "approved"</verify>
  <done>User has approved the visual design, narrative copy, layout, and mobile responsiveness of the complete homeowner page</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- All 9 sections render in narrative order at /h/[slug]
- Appreciation chart renders with olive-to-gold gradient
- S&P callout is a separate card below chart
- Page is mobile-responsive at 375px
- 404 renders for invalid/draft slugs
- Visual verification by user confirms design quality
</verification>

<success_criteria>
- Recharts installed and appreciation chart rendering
- Chart shows smooth curve from purchase price to current value
- S&P callout card displays below chart with comparison data
- All 9 sections of the complete homeowner page render correctly
- Mobile-first design verified at 375px viewport
- User approves visual quality of the complete page
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-homeowner-page/02-03-SUMMARY.md`
</output>
