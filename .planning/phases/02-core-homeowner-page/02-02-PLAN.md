---
phase: 02-core-homeowner-page
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/components/homeowner/PurchaseStory.tsx
  - src/components/homeowner/CurrentValue.tsx
  - src/components/homeowner/EquityGained.tsx
  - src/components/homeowner/EarningsReframe.tsx
  - src/components/homeowner/RentVsOwn.tsx
  - src/components/homeowner/NetProceeds.tsx
  - src/components/homeowner/HomeownerPage.tsx
autonomous: true
requirements: [DATA-01, DATA-02, DATA-04, DATA-05, DATA-07, NARR-02]

must_haves:
  truths:
    - "Page shows purchase price and purchase date in 'Your story begins' section"
    - "Page shows current home value in 'Today, your home is worth' section"
    - "Page shows equity gained in 'That means you've built' section"
    - "Page shows earnings per week as the hero number in 'Another way to look at it' section"
    - "Page shows rent vs own side-by-side comparison with SB7 problem/triumph framing"
    - "Page shows net proceeds headline number with expandable breakdown"
    - "Every data section opens with a narrative sentence before showing the big number (SB7)"
    - "Narrative energy escalates through the sections"
  artifacts:
    - path: "src/components/homeowner/PurchaseStory.tsx"
      provides: "Purchase price and date section"
      min_lines: 15
    - path: "src/components/homeowner/CurrentValue.tsx"
      provides: "Current value reveal section"
      min_lines: 15
    - path: "src/components/homeowner/EquityGained.tsx"
      provides: "Equity gained section"
      min_lines: 15
    - path: "src/components/homeowner/EarningsReframe.tsx"
      provides: "Earnings per week section"
      min_lines: 15
    - path: "src/components/homeowner/RentVsOwn.tsx"
      provides: "Rent vs own comparison"
      min_lines: 20
    - path: "src/components/homeowner/NetProceeds.tsx"
      provides: "Net proceeds with expandable breakdown"
      min_lines: 30
    - path: "src/components/homeowner/HomeownerPage.tsx"
      provides: "Updated orchestrator with all data sections wired"
      min_lines: 50
  key_links:
    - from: "src/components/homeowner/HomeownerPage.tsx"
      to: "all section components"
      via: "imports and renders in narrative order"
      pattern: "import.*from.*homeowner/"
    - from: "src/components/homeowner/NetProceeds.tsx"
      to: "useState"
      via: "expandable breakdown toggle"
      pattern: "useState"
---

<objective>
Build all six data section components (purchase story, current value, equity gained, earnings reframe, rent vs own, net proceeds) and wire them into the HomeownerPage orchestrator. This is the heart of the homeowner experience -- every section follows the SB7 hero-number-narrative pattern with escalating energy.

Purpose: Transforms the page from a shell into a complete financial story, displaying all computed metrics in a narrative arc that builds to the net proceeds climax.
Output: Six section components rendering real data, wired into HomeownerPage in the correct narrative order.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-homeowner-page/02-CONTEXT.md
@.planning/phases/02-core-homeowner-page/02-RESEARCH.md
@.planning/phases/02-core-homeowner-page/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -- executor uses these directly -->

From src/lib/format.ts:
```typescript
export function formatCurrency(value: number, decimals?: number): string;
export function formatPercent(value: number, decimals?: number): string;
```

From src/components/homeowner/SectionWrapper.tsx:
```typescript
export function SectionWrapper({ bg, children }: { bg: "cream" | "white"; children: React.ReactNode }): JSX.Element;
```

From src/lib/types.ts (relevant subset):
```typescript
export interface HomeownerRecord {
  purchasePrice: number;
  purchaseDate: string; // ISO date "YYYY-MM-DD"
  currentValue: number;
  address: string;
}

export interface HomeownerMetrics {
  equityGained: number;
  earningsPerWeek: number;
  earningsPerMonth: number;
  earningsPerDay: number;
  rentVsOwn: { totalRentWouldHavePaid: number; equityBuilt: number; netBenefit: number; };
  netProceeds: { gross: number; agentCommission: number; closingCosts: number; mortgagePayoff: number; net: number; };
  ownershipMonths: number;
  ownershipYears: number;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build first four data sections (purchase story through earnings reframe)</name>
  <files>src/components/homeowner/PurchaseStory.tsx, src/components/homeowner/CurrentValue.tsx, src/components/homeowner/EquityGained.tsx, src/components/homeowner/EarningsReframe.tsx</files>
  <action>
    All four components follow the same SB7 pattern: narrative intro line (text-canyon font-body), hero number (text-charcoal font-heading text-4xl md:text-5xl lg:text-6xl font-bold), supporting narrative (text-charcoal/70 font-body text-lg max-w-md mx-auto). Each uses SectionWrapper with alternating backgrounds.

    **PurchaseStory** (bg="white"):
    - Props: `{ purchasePrice: number; purchaseDate: string }`
    - Narrative intro: "Your story begins on..." or similar warm SB7 opener
    - Hero display: formatted purchase date (using date-fns `format(parseISO(purchaseDate), "MMMM d, yyyy")`)
    - Supporting: "when you made one of the biggest investments of your life" + formatted purchase price
    - This is the calm, contextual opening -- lowest energy section

    **CurrentValue** (bg="cream"):
    - Props: `{ currentValue: number }`
    - Narrative intro: "Today, your home is worth..."
    - Hero number: `formatCurrency(currentValue)` in large bold text
    - Supporting narrative builds confidence: acknowledge the market journey
    - Energy level: building -- this is the first "wow" moment

    **EquityGained** (bg="white"):
    - Props: `{ equityGained: number }`
    - Narrative intro: "That means you've built..."
    - Hero number: `formatCurrency(equityGained)`
    - Supporting: "in equity since you first opened the front door" or similar
    - Energy level: confident -- connecting the dots

    **EarningsReframe** (bg="cream"):
    - Props: `{ earningsPerWeek: number; earningsPerDay: number; earningsPerMonth: number }`
    - Narrative intro: "Another way to look at it..."
    - Hero number: `formatCurrency(earningsPerWeek)` + "/week" suffix
    - Supporting: Show per-month and per-day as secondary stats below
    - Energy level: peak reframing moment -- "Your home earned $X/week"

    Use responsive text sizing to prevent overflow on 375px: hero numbers use `text-4xl md:text-5xl lg:text-6xl`. Test mentally with 7-digit values like $1,247,500.
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>All four section components render with SB7 narrative pattern, correct formatting, responsive text sizing, and alternating backgrounds</done>
</task>

<task type="auto">
  <name>Task 2: Build rent-vs-own and net proceeds sections, wire all into HomeownerPage</name>
  <files>src/components/homeowner/RentVsOwn.tsx, src/components/homeowner/NetProceeds.tsx, src/components/homeowner/HomeownerPage.tsx</files>
  <action>
    **RentVsOwn** (bg="white"):
    - Props: `{ rentVsOwn: HomeownerMetrics["rentVsOwn"] }`
    - Narrative intro: "What if you had rented instead?" or similar SB7 problem framing
    - Side-by-side layout (stacked on mobile, side-by-side on md+):
      - Left card: "If you rented" -- shows `formatCurrency(totalRentWouldHavePaid)` with "gone" framing (money disappeared, SB7 villain). Use canyon/muted styling.
      - Right card: "Because you own" -- shows `formatCurrency(equityBuilt)` with "built" framing (wealth created, SB7 triumph). Use olive/gold styling.
    - Below cards: net benefit summary line
    - Per locked decision: SB7 problem/triumph framing -- renting is the villain (money gone), owning is the victory (money built)

    **NetProceeds** (bg="cream"):
    - Props: `{ netProceeds: HomeownerMetrics["netProceeds"] }`
    - Narrative intro: "If you sold today, you'd walk away with..."
    - Hero number: `formatCurrency(netProceeds.net)` -- single triumphant headline
    - Expandable breakdown toggle using `useState`:
      - "See the breakdown" / "Hide breakdown" button (`text-olive underline`)
      - Expanded view shows: Sale price, minus Agent commission, minus Closing costs, minus Mortgage payoff, equals Net proceeds
      - Left-aligned within max-w-sm mx-auto, flex justify-between for each line item
    - Per locked decision: single headline number with expandable breakdown
    - This is the climax -- highest narrative energy, triumphant tone

    **Update HomeownerPage:**
    - Remove all placeholder divs for data sections
    - Import and render all six section components in exact narrative order:
      1. HeroSection
      2. PersonalNote
      3. PurchaseStory (props from record)
      4. CurrentValue (props from record)
      5. EquityGained (props from metrics)
      6. EarningsReframe (props from metrics)
      7. [Space for AppreciationChart -- placeholder comment "Chart section added in Plan 03"]
      8. RentVsOwn (props from metrics)
      9. NetProceeds (props from metrics)
    - Pass correct props from record and metrics to each component
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>RentVsOwn shows side-by-side contrast with SB7 framing. NetProceeds shows headline number with working expandable breakdown. HomeownerPage renders all sections in correct narrative order with escalating energy.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no type errors
- All six data sections render in the correct narrative order
- Every section follows SB7 pattern: narrative intro, hero number, supporting copy
- NetProceeds expandable breakdown toggles correctly
- RentVsOwn uses side-by-side problem/triumph layout
</verification>

<success_criteria>
- All six data section components created and rendering
- SB7 narrative pattern followed in every section (narrative before numbers)
- Escalating narrative energy from calm (purchase story) to triumphant (net proceeds)
- Rent vs own uses side-by-side SB7 problem/triumph framing
- Net proceeds has expandable breakdown with toggle
- HomeownerPage renders the complete narrative arc in correct order
- Mobile-first responsive text sizing prevents overflow
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-homeowner-page/02-02-SUMMARY.md`
</output>
