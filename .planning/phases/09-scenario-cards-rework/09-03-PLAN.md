---
phase: 09-scenario-cards-rework
plan: 03
type: execute
wave: 3
depends_on: ["09-02"]
files_modified:
  - src/components/homeowner/StayAndInvestCard.tsx
  - src/components/homeowner/MoveAndKeepAsRentalCard.tsx
  - src/components/homeowner/ScenarioCards.tsx
  - src/components/homeowner/ScenarioCards.test.tsx
autonomous: true
requirements: [SCNR-01, SCNR-07, SCNR-08, SCNR-09, SCNR-10]

must_haves:
  truths:
    - "Stay & Invest card shows HELOC available, monthly HELOC cost, investment property framing, and tax advantages callout"
    - "Move & Keep as Rental card shows the effective interest rate as the hero metric"
    - "Move & Keep as Rental card shows rental income, cash flow, and mini mortgage calculator"
    - "Each scenario card has its own contextual CTA (lender connect, STR Analyzer, Josh & Jacqui)"
    - "All four new scenario cards render in ScenarioCards.tsx, replacing the old four cards"
    - "Market rate slider above all cards controls the baseline rate"
    - "ScenarioCards tests updated for new card titles and structure"
  artifacts:
    - path: "src/components/homeowner/StayAndInvestCard.tsx"
      provides: "Stay & Invest scenario card with HELOC and tax callout"
      exports: ["StayAndInvestCard"]
    - path: "src/components/homeowner/MoveAndKeepAsRentalCard.tsx"
      provides: "Move & Keep as Rental card with effective interest rate hero metric"
      exports: ["MoveAndKeepAsRentalCard"]
    - path: "src/components/homeowner/ScenarioCards.tsx"
      provides: "Refactored parent component rendering all 4 new scenario cards"
      exports: ["ScenarioCards"]
    - path: "src/components/homeowner/ScenarioCards.test.tsx"
      provides: "Updated tests for new scenario card structure"
      contains: "Stay & Build Wealth"
  key_links:
    - from: "src/components/homeowner/MoveAndKeepAsRentalCard.tsx"
      to: "src/lib/calculations.ts"
      via: "calcEffectiveInterestRate for hero metric"
      pattern: "calcEffectiveInterestRate"
    - from: "src/components/homeowner/ScenarioCards.tsx"
      to: "src/components/homeowner/StayAndBuildCard.tsx"
      via: "import and render"
      pattern: "import.*StayAndBuildCard"
    - from: "src/components/homeowner/ScenarioCards.tsx"
      to: "src/lib/horizon-dates.ts"
      via: "getHorizonDates for date-based horizons"
      pattern: "getHorizonDates"
---

<objective>
Build the final two scenario cards (Stay & Invest, Move & Keep as Rental), wire all four new cards into ScenarioCards.tsx replacing the old cards, and update tests.

Purpose: Complete the scenario cards rework with all four homeowner decision paths, the effective interest rate hero metric, contextual CTAs, and date-based horizons -- the culmination of Phase 9.

Output: Fully functional ScenarioCards section with four new cards, dynamic date horizons, market rate slider, mini mortgage calculators, effective interest rate, and updated test coverage.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-scenario-cards-rework/09-CONTEXT.md
@.planning/phases/09-scenario-cards-rework/09-RESEARCH.md
@.planning/phases/09-scenario-cards-rework/09-01-SUMMARY.md
@.planning/phases/09-scenario-cards-rework/09-02-SUMMARY.md

<interfaces>
<!-- All types and components from Plans 01 and 02 -->

From src/lib/types.ts:
```typescript
export type HorizonYears = 0 | 5 | 10 | 15;
export interface StayAndInvestProjection {
  years: HorizonYears; date: Date; homeValue: number; mortgageBalance: number;
  equity: number; helocAvailable: number; monthlyHelocCost: number;
}
export interface MoveAndKeepProjection {
  years: HorizonYears; date: Date; homeValue: number; mortgageBalance: number;
  monthlyRent: number; oldMortgagePayment: number; monthlyCashFlow: number;
}
```

From src/lib/calculations.ts:
```typescript
export function calcEffectiveInterestRate(loanAmount: number, targetPayment: number, termYears?: number, tolerance?: number, maxIterations?: number): number | null;
export function calcMortgagePayment(loanAmount: number, annualRate: number, termYears: number): number;
export function calcRemainingBalance(loanAmount: number, annualRate: number, termYears: number, monthsElapsed: number): number;
export const NEW_PROJECTION_HORIZONS = [0, 5, 10, 15] as const;
```

From src/lib/horizon-dates.ts:
```typescript
export interface HorizonDate { label: string; shortLabel: string; years: HorizonYears; date: Date; }
export function getHorizonDates(now: Date): HorizonDate[];
export const SELL_COMMISSION_RATE: number;
export const SELL_CLOSING_RATE: number;
```

From src/components/homeowner/MiniMortgageCalculator.tsx (Plan 02):
```typescript
interface MiniMortgageCalculatorProps {
  marketRate: number;
  defaultHomePrice?: number;
  onPaymentChange?: (payment: number, loanAmount: number, rate: number) => void;
}
export function MiniMortgageCalculator(props: MiniMortgageCalculatorProps): JSX.Element;
```

From src/components/homeowner/ScenarioCard.tsx (updated in Plan 02):
```typescript
// Now accepts HorizonYears and HorizonDate[]
interface ScenarioCardProps {
  title: string;
  heroMetric: string;
  implication: string;
  horizonContent?: Record<HorizonYears, HorizonContent>;
  horizons?: HorizonDate[];
  children?: ReactNode;
}
```

From src/components/homeowner/HorizonTabs.tsx (updated in Plan 02):
```typescript
// Now accepts HorizonDate[] and HorizonYears
```

From src/components/homeowner/StayAndBuildCard.tsx (Plan 02):
```typescript
export function StayAndBuildCard({ record, metrics, marketRate, horizons }: StayAndBuildCardProps): JSX.Element;
```

From src/components/homeowner/SellAndMoveUpCard.tsx (Plan 02):
```typescript
export function SellAndMoveUpCard({ record, metrics, marketRate, horizons }: SellAndMoveUpCardProps): JSX.Element;
```

From src/lib/defaults.ts:
```typescript
export const DEFAULTS = { helocLtvMax: 0.8, appreciationRate: 0.04, rentGrowthRate: 0.03, rentYieldMonthly: 0.008/12, ... };
```

From src/lib/narrative.ts:
```typescript
export type NarrativeVoice = "recent" | "midterm" | "longterm";
export const scenarioCardsCopy: Record<NarrativeVoice, { intro: string }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: StayAndInvestCard and MoveAndKeepAsRentalCard</name>
  <files>src/components/homeowner/StayAndInvestCard.tsx, src/components/homeowner/MoveAndKeepAsRentalCard.tsx</files>
  <action>
    1. **Create StayAndInvestCard.tsx** -- "Stay & Invest":
    - Props: `{ record: HomeownerRecord; metrics: HomeownerMetrics; marketRate: number; horizons: HorizonDate[] }`
    - Compute projections via useMemo for all 4 horizons (0, 5, 10, 15):
      - For each horizon: homeValue = currentValue * (1 + appreciationRate)^year, mortgageBalance = calcRemainingBalance(...), equity = homeValue - mortgageBalance, helocAvailable = max(0, homeValue * helocLtvMax - mortgageBalance), monthlyHelocCost = helocAvailable * (marketRate / 12)
      - Year 0: use current values directly (same approach as StayAndBuildCard)
    - Build horizonContent:
      - heroMetric: formatCurrency(helocAvailable) -- what they could access
      - implication: "Leverage {helocAvailable} without selling your home"
      - details: Home value, Total equity, "HELOC available (80% LTV)", "Monthly HELOC cost (interest-only)"
    - Children content (rendered inside expanded area):
      a. **Investment property framing**: "Your HELOC could be a down payment on a rental property -- turning your home's equity into a cash-flowing asset"
      b. **Tax advantages callout**: A subtle olive-tinted box: "W-2 earners: Real estate depreciation (including bonus depreciation under OBBBA) can offset your taxable income. Cost segregation accelerates this -- talk to a CPA about your situation."
      c. **CTA 1**: "Try our STR Analyzer" -- link to Josh's STR Analyzer tool. Use href to the deployed URL. The STR Analyzer link IS allowed even though other external links are removed (it's Josh's own tool). Per CLAUDE.md the Vercel deploy is primary for stranalyzer. Use a placeholder constant `const STR_ANALYZER_URL = "https://stranalyzer.vercel.app"` that can be updated.
      d. **CTA 2**: SB7 warm closing: "Josh and Jacqui invest in short-term rentals themselves -- let's talk about how your equity could work for you." With inline tel link to (480) 369-8280
    - Default active horizon to 5 (the future is the interesting part)

    2. **Create MoveAndKeepAsRentalCard.tsx** -- "Move & Keep as Rental":
    - Props: `{ record: HomeownerRecord; metrics: HomeownerMetrics; marketRate: number; horizons: HorizonDate[] }`
    - Extract fields: monthlyRentEstimate (from record or DEFAULTS.rentYieldMonthly fallback), loanAmount, interestRate, loanTerm, ownershipMonths
    - Compute old mortgage payment: `calcMortgagePayment(loanAmount, interestRate, loanTerm)`
    - Compute projections via useMemo for all 4 horizons:
      - Year 0: monthlyRent = monthlyRentEstimate, monthlyCashFlow = monthlyRent - oldMortgagePayment
      - Years 5/10/15: monthlyRent grows at rentGrowthRate, homeValue appreciates, mortgageBalance pays down. monthlyCashFlow = projected rent - oldMortgagePayment
    - Build horizonContent:
      - heroMetric: formatCurrency(monthlyCashFlow) + "/mo" -- cash flow is the collapsed hero
      - implication: "Your current home generates {cashFlow}/mo in rental cash flow"
      - details: "Monthly rental income", "Current mortgage payment", "Monthly cash flow", "Projected home value"
    - **Mini mortgage calculator** for the new home: render MiniMortgageCalculator with `onPaymentChange` callback
    - **Effective interest rate hero metric** (the conversation changer):
      - Track `newMortgagePayment` and `newLoanAmount` from MiniMortgageCalculator via `onPaymentChange` callback stored in local useState
      - Compute: `rentalCashFlow = monthlyRentEstimate - oldMortgagePayment` (if positive, rental covers some of new mortgage)
      - Actually per CONTEXT: `rentalCashFlow = monthlyRentEstimate` (gross rental income). Then `outOfPocket = newMortgagePayment - rentalCashFlow` (clamped to >= 0)
      - Wait -- re-read CONTEXT: "Take new home mortgage payment, subtract rental cash flow = out-of-pocket payment" and "cash flow (rent minus old mortgage)". The cash flow displayed on the card is rent minus old mortgage. But the effective rate uses the FULL rental income to offset the new mortgage, since the old mortgage still exists on the rental property. Let me re-read...
      - Actually, the effective rate concept is: you KEEP the old home (still paying old mortgage). You rent it out (getting monthlyRentEstimate). You buy NEW home (new mortgage payment). Your total cost = old mortgage + new mortgage - rental income. The effective rate reframes this as: what rate on the new mortgage alone would produce your actual net cost?
      - More precisely from CONTEXT: "Take new home mortgage payment, subtract rental cash flow = out-of-pocket payment." Where rental cash flow = monthlyRentEstimate - old mortgage. So: outOfPocket = newMortgagePayment - (monthlyRentEstimate - oldMortgagePayment) = newMortgagePayment - monthlyRentEstimate + oldMortgagePayment
      - Hmm, but the CONTEXT example says: "$3,200 mortgage - $800 cash flow = $2,400 out-of-pocket." The $800 is the rental cash flow (rent minus old mortgage). So outOfPocket = newMortgagePayment - rentalCashFlow where rentalCashFlow = rent - oldMortgage.
      - Compute: `rentalCashFlow = monthlyRentEstimate - oldMortgagePayment`. `outOfPocket = Math.max(0, newMortgagePayment - rentalCashFlow)`.
      - Wait, if rentalCashFlow is negative (mortgage > rent), then outOfPocket > newMortgagePayment. That doesn't make sense for the effective rate -- you'd want effectiveRate > actualRate. Let me think...
      - If rentalCashFlow = rent - oldMortgage = negative, that means the rental doesn't cover the old mortgage. outOfPocket = newMortgage - (negative number) = newMortgage + loss. The effective rate would be HIGHER than actual, which is correct -- the rental isn't helping, it's costing you.
      - But that contradicts the purpose ("reframes rates are too high objection"). When rental cash flow is negative, this scenario doesn't help. So guard: only show effective rate when rentalCashFlow > 0.
      - `effectiveRate = outOfPocket > 0 ? calcEffectiveInterestRate(newLoanAmount, outOfPocket, 30) : 0`
      - Display: When no new home price entered yet, show "--" with "Enter a new home price to see your effective rate"
      - When effective rate computed, show it as the hero metric in a prominent olive box: "Your effective rate: X.XX%"
      - When rentalCashFlow <= 0, show the actual rate with note "Rental income doesn't fully cover your current mortgage -- talk to us about strategies"
      - When outOfPocket <= 0 (rental fully covers new mortgage), show "0.00%" with "Rental income covers your new payment"
    - **CTA**: "This is a conversation worth having -- call or text Josh" with inline tel/sms links to (480) 369-8280
    - Default active horizon to 0 (Now) for this card -- the effective rate concept works best grounded in today's numbers
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx tsc --noEmit</automated>
  </verify>
  <done>StayAndInvestCard renders HELOC projections, tax callout, STR Analyzer link, and warm CTA. MoveAndKeepAsRentalCard renders rental cash flow, MiniMortgageCalculator, effective interest rate hero metric, and contextual CTA. Both compile cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire all four cards into ScenarioCards.tsx and update tests</name>
  <files>src/components/homeowner/ScenarioCards.tsx, src/components/homeowner/ScenarioCards.test.tsx</files>
  <action>
    1. **Refactor ScenarioCards.tsx** to render the four new cards:
    - Remove imports: `calcHoldAndRentProjections`, `calcMoveUpProjections`, `calcEquityPlayProjections`, `calcComboScenarioProjections`, `ComboScenario`
    - Add imports: `StayAndBuildCard`, `SellAndMoveUpCard`, `StayAndInvestCard`, `MoveAndKeepAsRentalCard`, `getHorizonDates` from `@/lib/horizon-dates`
    - Remove all the old useMemo projection computations (holdAndRentProjections, moveUpProjections, equityPlayProjections, comboProjections)
    - Remove all the old horizonContent Record maps (holdHorizonContent, moveUpHorizonContent, equityPlayHorizonContent)
    - Remove the old rate lock narrative logic and refi payment logic (these are no longer card-level concerns -- individual cards handle their own narratives)
    - Keep: `useState(currentMarketRate)` for the slider, `MarketRateSlider` component
    - Add: `const horizons = useMemo(() => getHorizonDates(new Date()), [])` -- compute once on mount
    - Render the four new cards inside the grid, passing `record`, `metrics`, `marketRate`, and `horizons` to each:
      ```tsx
      <StayAndBuildCard record={record} metrics={metrics} marketRate={marketRate} horizons={horizons} />
      <SellAndMoveUpCard record={record} metrics={metrics} marketRate={marketRate} horizons={horizons} />
      <StayAndInvestCard record={record} metrics={metrics} marketRate={marketRate} horizons={horizons} />
      <MoveAndKeepAsRentalCard record={record} metrics={metrics} marketRate={marketRate} horizons={horizons} />
      ```
    - Keep the section header "Where could you go from here?" and voice-adapted intro
    - Keep SectionWrapper and AnimatedSection structure
    - The `voice` prop is still used for the scenarioCardsCopy intro text

    2. **Update ScenarioCards.test.tsx**:
    - Update the "renders three cards with correct titles" test to check for four new titles: "Stay & Build Wealth", "Sell & Move Up", "Stay & Invest", "Move & Keep as Rental"
    - Update "shows horizon tabs when card is expanded" test: click "Stay & Build Wealth", check for date-based tabs (the exact dates depend on when tests run, so check for the general pattern -- look for elements with year text like "2031" or month abbreviation)
    - Remove "shows sell-to-buy education" test (that content no longer exists in the new cards)
    - Add test: "shows effective interest rate section in Move & Keep as Rental" -- expand the card, verify "effective" text appears
    - Add test: "shows tax advantages callout in Stay & Invest" -- expand the card, verify depreciation text appears
    - Add test: "renders STR Analyzer link in Stay & Invest" -- verify link text
    - Keep: section header test, voice-appropriate intro tests, market rate slider test
    - Remove: rate lock advantage test from ScenarioCards level (rate lock is now handled differently)
    - Mock `getHorizonDates` if needed to avoid date-dependent test failures, or accept that tests will show current dates

    3. **Remove dead code**: The `ComboScenario.tsx` component is no longer imported. Do NOT delete it yet (keep as dead code per project pattern from Phase 6 -- "Kept SP500Callout.tsx and RentVsOwn.tsx as dead code for reference"). Just remove the import from ScenarioCards.tsx.
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx vitest run src/components/homeowner/ScenarioCards.test.tsx -x</automated>
  </verify>
  <done>ScenarioCards.tsx renders four new scenario cards with market rate slider and date-based horizons. Old card code removed. ComboScenario import removed (file kept as dead code). All ScenarioCards tests pass with new card titles, horizon tabs, effective rate section, tax callout, and STR Analyzer link verified.</done>
</task>

</tasks>

<verification>
- All tests pass: `cd ~/Projects/homeowner-journey-map && npx vitest run`
- TypeScript compiles: `cd ~/Projects/homeowner-journey-map && npx tsc --noEmit`
- ScenarioCards renders four new cards with correct titles
- Market rate slider controls all card calculations
- MiniMortgageCalculator instances have independent state
- Effective interest rate displays correctly in Move & Keep as Rental
- Date-based horizon tabs show formatted dates
- Each card has its contextual CTA
</verification>

<success_criteria>
- Four scenario cards render: Stay & Build Wealth, Sell & Move Up, Stay & Invest, Move & Keep as Rental
- Dynamic date horizons show actual dates (e.g., Mar '26 | Mar '31 | Mar '36 | Mar '41) with year-only mobile fallback
- One market rate slider above all cards; mini calculators override independently
- Sell & Move Up shows net proceeds with "cost to sell*" (6% + 1%), mini calculator with down payment presets, lender CTA
- Stay & Invest shows HELOC, monthly cost, tax callout, STR Analyzer link, Josh & Jacqui CTA
- Move & Keep as Rental shows effective interest rate as hero metric, rental cash flow, mini calculator, contextual CTA
- Each scenario has contextual CTAs (lender connect, STR Analyzer, talk to Josh & Jacqui)
- Old scenario cards replaced (ComboScenario file kept as dead code)
- All tests pass including updated ScenarioCards tests
</success_criteria>

<output>
After completion, create `.planning/phases/09-scenario-cards-rework/09-03-SUMMARY.md`
</output>
