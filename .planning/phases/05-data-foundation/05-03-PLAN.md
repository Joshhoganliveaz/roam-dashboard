---
phase: 05-data-foundation
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - src/components/homeowner/ConditionPicker.tsx
  - src/components/homeowner/CurrentValue.tsx
  - src/components/homeowner/HomeownerPage.tsx
  - src/components/homeowner/AppreciationChart.tsx
  - src/components/homeowner/CountUpNumber.tsx
  - src/lib/hooks/useCounterRoll.ts
  - src/components/homeowner/EquityGained.tsx
  - src/components/homeowner/EarningsReframe.tsx
  - src/components/homeowner/NetProceeds.tsx
  - src/components/homeowner/SP500Callout.tsx
  - src/components/homeowner/RentVsOwn.tsx
autonomous: false
requirements: [VALU-04, CROM-03, CROM-04]

must_haves:
  truths:
    - "Homeowner sees three condition choices inline with the value section"
    - "Selecting a condition reveals the home value and cascades counter-roll animations down the page"
    - "Counter-roll animation completes in 3-4 seconds total, each section animating sequentially"
    - "Appreciation chart shows real Cromford market data when available"
    - "Appreciation chart shows shaped fallback curve (not straight line) when Cromford data is absent"
    - "Changing condition shifts the entire appreciation curve proportionally"
  artifacts:
    - path: "src/components/homeowner/ConditionPicker.tsx"
      provides: "Inline condition selector with label + description for each option"
      exports: ["ConditionPicker"]
    - path: "src/lib/hooks/useCounterRoll.ts"
      provides: "Hook animating between old and new numeric values using Motion animate()"
      exports: ["useCounterRoll"]
    - path: "src/components/homeowner/AppreciationChart.tsx"
      provides: "Chart rendering real Cromford data or shaped fallback, with condition-aware curve shifting"
      contains: "cromfordData"
    - path: "src/components/homeowner/CountUpNumber.tsx"
      provides: "Updated component supporting both initial scroll count-up and condition-change counter-roll"
      contains: "useCounterRoll"
  key_links:
    - from: "src/components/homeowner/ConditionPicker.tsx"
      to: "src/components/homeowner/HomeownerPage.tsx"
      via: "onChange triggers setCondition in HomeownerPage"
      pattern: "setCondition"
    - from: "src/components/homeowner/HomeownerPage.tsx"
      to: "src/components/homeowner/CountUpNumber.tsx"
      via: "Condition change causes new metrics values to flow to CountUpNumber components"
      pattern: "conditionVariants.*condition"
    - from: "src/components/homeowner/AppreciationChart.tsx"
      to: "src/lib/cromford.ts"
      via: "Uses applyCurveConditionFactor to shift Cromford data by condition"
      pattern: "applyCurveConditionFactor|generateShapedCurve"
---

<objective>
Build the interactive condition picker, cascading counter-roll animation, and condition-aware appreciation chart with real Cromford data support.

Purpose: This is the user-facing interactive experience -- the homeowner picks their home's condition, watches numbers cascade-animate to new values, and sees the appreciation chart shift. It transforms the page from static to interactive.

Output: Working condition picker inline with value section, theatrical counter-roll animation across all data sections, appreciation chart with Cromford data or realistic shaped fallback, condition-aware curve shifting.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-data-foundation/05-CONTEXT.md
@.planning/phases/05-data-foundation/05-RESEARCH.md
@.planning/phases/05-data-foundation/05-01-SUMMARY.md
@.planning/phases/05-data-foundation/05-02-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/types.ts (created in Plan 01):
```typescript
export type Condition = "original" | "updated" | "remodeled";
export interface ConditionVariants {
  original: HomeownerMetrics;
  updated: HomeownerMetrics;
  remodeled: HomeownerMetrics;
}
export interface CromfordDataPoint { date: string; pricePsf: number; }
export interface CromfordData {
  city: string;
  chartType: string;
  dataPoints: CromfordDataPoint[];
  extractedAt: string;
}
```

From src/components/homeowner/HomeownerPage.tsx (updated in Plan 01):
```typescript
// Has useState<Condition> for condition tracking
// Has conditionVariants, conditionValues props
// Derives metrics = conditionVariants[condition]
```

<!-- From Plan 02 outputs -->

From src/lib/cromford.ts (created in Plan 02):
```typescript
export function generateShapedCurve(purchasePrice, currentValue, months, annualRate): ChartPoint[];
export function applyCurveConditionFactor(dataPoints: CromfordDataPoint[], factor: number): CromfordDataPoint[];
export function getConditionFactor(condition, valueLow, valueHigh, homeSqft, currentPsf): number;
```

<!-- Existing animation patterns -->

From src/lib/hooks/useCountUp.ts:
```typescript
// Counts from 0 to target once on scroll-into-view
// Uses hasAnimated.current = true to prevent replay
// Must NOT conflict with new counter-roll (different trigger)
```

From src/components/homeowner/CountUpNumber.tsx:
```typescript
// Uses useCountUp + useInView for initial scroll reveal
// Needs to also support counter-roll between values on condition change
```

From motion/react:
```typescript
// animate() imperative API available for tweening numeric values
import { animate } from "motion/react";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Condition picker, counter-roll hook, and cascading animation wiring</name>
  <files>src/components/homeowner/ConditionPicker.tsx, src/lib/hooks/useCounterRoll.ts, src/components/homeowner/CountUpNumber.tsx, src/components/homeowner/CurrentValue.tsx, src/components/homeowner/HomeownerPage.tsx, src/components/homeowner/EquityGained.tsx, src/components/homeowner/EarningsReframe.tsx, src/components/homeowner/NetProceeds.tsx, src/components/homeowner/SP500Callout.tsx, src/components/homeowner/RentVsOwn.tsx</files>
  <action>
    1. Create `src/lib/hooks/useCounterRoll.ts`:
       - Uses Motion's `animate()` imperative API to tween between old and new numeric values
       - Signature: `useCounterRoll(target: number, delay: number = 0, duration: number = 500): number`
       - Tracks previous value in useRef; when target changes, animates from old to new after delay
       - Uses "easeInOut" easing for odometer feel
       - Returns the current display value (animated intermediate or final)
       - First render: returns target directly (no animation on mount -- initial scroll animation handles that)

    2. Create `src/components/homeowner/ConditionPicker.tsx`:
       - Props: `selected: Condition`, `onChange: (c: Condition) => void`, `revealed: boolean`
       - Three card-style buttons, stacked vertically on mobile, horizontal row on sm+
       - Each shows label + one-liner description:
         - "Mostly original" / "Original fixtures, no major renovations"
         - "Made some updates" / "Kitchen or bath refresh, newer flooring"
         - "Completely remodeled" / "Full renovation, modern finishes throughout"
       - Selected state: olive border, olive/10 background, subtle shadow
       - Unselected: canyon/20 border, hover canyon/40
       - Values are NOT shown on the buttons (per user decision -- selecting reveals the value)
       - Default is "Made some updates" (middle, pre-selected)
       - Branded styling: rounded-xl, DM Sans body text, charcoal/60 descriptions

    3. Update `src/components/homeowner/CurrentValue.tsx`:
       - Add ConditionPicker inline below the narrative intro, above the value display
       - Receives condition, setCondition, conditionValues props from HomeownerPage
       - When condition changes, the value display updates (CountUpNumber handles the animation)
       - The value is "revealed" by the condition selection -- initial state shows picker but value is visible with default condition

    4. Update `src/components/homeowner/CountUpNumber.tsx`:
       - Add support for counter-roll mode alongside existing scroll count-up
       - After initial scroll animation completes (hasAnimated.current = true), subsequent value changes trigger useCounterRoll
       - Accept `cascadeDelay: number` prop (default 0) for theatrical staggering
       - When value prop changes after initial animation, use useCounterRoll to animate from old to new
       - Total cascade budget: 3-4 seconds across ~6 sections = ~500-600ms per section
       - All sections animate regardless of viewport position (per user decision)

    5. Wire cascading animation in `src/components/homeowner/HomeownerPage.tsx`:
       - Pass cascadeDelay to each data section based on section index:
         - CurrentValue: delay 0ms (starts immediately on condition change)
         - EquityGained: delay 500ms
         - EarningsReframe: delay 1000ms
         - SP500Callout: delay 1500ms (approximate, depends on section count)
         - RentVsOwn: delay 2000ms
         - NetProceeds: delay 2500ms
       - Pass condition and setCondition to CurrentValue for the picker

    6. Update each data section component (EquityGained, EarningsReframe, NetProceeds, SP500Callout, RentVsOwn):
       - Accept `cascadeDelay: number` prop
       - Pass cascadeDelay to their CountUpNumber components
       - No other changes needed -- they already receive metrics values from HomeownerPage
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Condition picker renders inline with value section. Selecting a condition updates the displayed value and triggers cascading counter-roll animations across all data sections. Animation completes in 3-4 seconds total with sequential section timing.</done>
</task>

<task type="auto">
  <name>Task 2: Appreciation chart with Cromford data and condition-aware curve shifting</name>
  <files>src/components/homeowner/AppreciationChart.tsx</files>
  <action>
    1. Update `src/components/homeowner/AppreciationChart.tsx`:
       - Add new props: `cromfordData: CromfordData | null`, `condition: Condition`, `valueLow: number`, `valueHigh: number`, `homeSqft: number`
       - Keep existing props for fallback: purchasePrice, currentValue, purchaseDate, annualizedAppreciation

    2. Data source selection:
       - If cromfordData exists with valid data points: use real Cromford data
         - Convert pricePsf to home values: `pricePsf * homeSqft * conditionFactor`
         - Get conditionFactor from `getConditionFactor(condition, valueLow, valueHigh, homeSqft, currentPsf)`
         - currentPsf = midpoint of Cromford data's most recent value (or derive from data)
       - If no Cromford data: use `generateShapedCurve()` from cromford.ts instead of the existing `generateChartData()`
         - This produces a realistic curve with seasonal variation and market cycle noise
         - Apply condition factor to shift the shaped curve proportionally

    3. Chart morphing on condition change:
       - Use `key={condition}` on AreaChart to force remount with animation when condition changes
       - Recharts' `isAnimationActive={true}`, `animationDuration={800}`, `animationEasing="ease-in-out"` handle the visual morph
       - The chart curve shifts smoothly to the new position (morph, not instant redraw)

    4. Update ReferenceDot labels:
       - "Today" marker shows the condition-adjusted current value (not the raw record value)
       - Purchase marker stays the same (purchase price doesn't change with condition)

    5. Keep the olive-to-gold gradient fill, existing axis formatting, and tooltip styling
       - The chart should look identical to the current one but with more realistic curve shape
       - When condition changes, the entire curve shifts up or down proportionally
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <done>Appreciation chart renders real Cromford data when available or realistic shaped fallback curve. Changing condition shifts the entire curve proportionally with smooth animation. Chart maintains branded olive-to-gold gradient styling.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of condition picker, cascading animation, and chart</name>
  <files>none</files>
  <action>
    Verify the complete interactive experience: condition picker, cascading counter-roll animation, and Cromford-powered appreciation chart.

    Steps:
    1. Start dev server: `cd ~/Projects/homeowner-journey-map && npm run dev`
    2. Create a test homeowner page via admin form with low comp value (e.g., 440000) and high comp value (e.g., 520000) and home sqft (e.g., 1800)
    3. Optionally upload a Cromford Report screenshot to test OCR extraction and preview
    4. Visit the homeowner page at /h/[slug]
    5. Verify condition picker shows three options inline with the value section
    6. Verify "Made some updates" is selected by default and shows the midpoint value
    7. Switch to "Mostly original" -- watch numbers cascade-animate downward section by section
    8. Switch to "Completely remodeled" -- watch numbers cascade-animate upward
    9. Verify the appreciation chart shifts with each condition change (smooth animation, not instant snap)
    10. Verify the total cascade animation takes approximately 3-4 seconds
    11. Test on mobile viewport (responsive condition picker layout)
  </action>
  <verify>User confirms visual behavior matches expectations</verify>
  <done>Josh approves condition picker UX, cascading animation timing, and chart shifting behavior</done>
</task>

</tasks>

<verification>
- `npx vitest run` passes (all tests green)
- `npx tsc --noEmit` passes (no type errors)
- Condition picker renders and triggers cascading animations visually
- Appreciation chart shows Cromford data or realistic shaped fallback
- Chart shifts with condition changes
</verification>

<success_criteria>
- Condition picker is inline with the value section with label + description per option
- Default is "Made some updates"; selecting reveals/updates the value
- Counter-roll animation cascades section by section over 3-4 seconds (odometer style)
- All sections animate regardless of viewport position
- Appreciation chart uses real Cromford data when available, shaped fallback otherwise
- Condition change shifts appreciation curve proportionally (morph, not redraw)
- Mobile-responsive condition picker layout
</success_criteria>

<output>
After completion, create `.planning/phases/05-data-foundation/05-03-SUMMARY.md`
</output>
