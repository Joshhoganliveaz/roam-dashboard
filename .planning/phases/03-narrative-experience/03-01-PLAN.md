---
phase: 03-narrative-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/narrative.ts
  - src/lib/narrative.test.ts
  - src/lib/milestones.ts
  - src/lib/milestones.test.ts
  - src/lib/hooks/useCountUp.ts
  - src/lib/hooks/useCountUp.test.ts
  - src/components/homeowner/AnimatedSection.tsx
  - src/components/homeowner/CountUpNumber.tsx
  - src/components/homeowner/MilestoneCallout.tsx
  - src/components/homeowner/HomeownerPage.tsx
  - src/components/homeowner/PurchaseStory.tsx
  - src/components/homeowner/CurrentValue.tsx
  - src/components/homeowner/EquityGained.tsx
  - src/components/homeowner/EarningsReframe.tsx
  - src/components/homeowner/RentVsOwn.tsx
  - src/components/homeowner/NetProceeds.tsx
  - src/components/homeowner/AppreciationChart.tsx
  - package.json
autonomous: true
requirements: [NARR-03, NARR-04, NARR-05]

must_haves:
  truths:
    - "Data sections animate on scroll -- sections fade/slide in as user scrolls down"
    - "Hero numbers count up from 0 to their target value with ease-in acceleration when scrolled into view"
    - "Key data sections (equity, earnings, net proceeds) have staggered element entrances (intro -> number -> copy)"
    - "Quieter sections (purchase story, personal note) animate as a single unit"
    - "Animations play only once -- scrolling back up does not replay"
    - "Narrative text adapts based on ownership duration (recent/midterm/longterm)"
    - "2-3 milestone callouts appear inline in relevant sections based on homeowner data"
    - "Appreciation chart draws itself on scroll trigger"
  artifacts:
    - path: "src/lib/narrative.ts"
      provides: "getNarrativeVoice() and copy maps for all sections"
      exports: ["getNarrativeVoice", "NarrativeVoice"]
    - path: "src/lib/narrative.test.ts"
      provides: "Boundary tests for voice selection"
    - path: "src/lib/milestones.ts"
      provides: "selectMilestones() logic"
      exports: ["selectMilestones", "Milestone"]
    - path: "src/lib/milestones.test.ts"
      provides: "Milestone selection and ranking tests"
    - path: "src/lib/hooks/useCountUp.ts"
      provides: "requestAnimationFrame counter with ease-in easing"
      exports: ["useCountUp"]
    - path: "src/components/homeowner/AnimatedSection.tsx"
      provides: "Motion-powered scroll-triggered section wrapper"
      exports: ["AnimatedSection"]
    - path: "src/components/homeowner/CountUpNumber.tsx"
      provides: "Animated number display component"
      exports: ["CountUpNumber"]
    - path: "src/components/homeowner/MilestoneCallout.tsx"
      provides: "Inline milestone badge component"
      exports: ["MilestoneCallout"]
  key_links:
    - from: "src/components/homeowner/AnimatedSection.tsx"
      to: "motion/react"
      via: "whileInView + viewport={{ once: true }}"
      pattern: "whileInView.*visible"
    - from: "src/components/homeowner/CountUpNumber.tsx"
      to: "src/lib/hooks/useCountUp.ts"
      via: "useCountUp hook with useInView trigger"
      pattern: "useCountUp.*useInView"
    - from: "src/components/homeowner/HomeownerPage.tsx"
      to: "src/lib/narrative.ts"
      via: "getNarrativeVoice(ownershipMonths) passed to sections"
      pattern: "getNarrativeVoice"
    - from: "src/components/homeowner/HomeownerPage.tsx"
      to: "src/lib/milestones.ts"
      via: "selectMilestones() results rendered as MilestoneCallout"
      pattern: "selectMilestones"
---

<objective>
Layer scroll-triggered animations, adaptive narrative voice, and milestone markers onto the existing homeowner page. Install motion library, create animation infrastructure (AnimatedSection, CountUpNumber, useCountUp hook), build narrative voice selection and copy maps for three ownership durations, implement milestone selection logic, and wire everything into the existing section components.

Purpose: Transform the static data page into a cinematic scroll-driven experience where sections reveal theatrically, numbers build anticipation, and the story adapts its voice to each homeowner's journey length.
Output: All existing sections animated with scroll triggers, hero numbers counting up, narrative text adapting by tenure, milestone callouts appearing inline.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-narrative-experience/03-CONTEXT.md
@.planning/phases/03-narrative-experience/03-RESEARCH.md
@.planning/phases/02-core-homeowner-page/02-01-SUMMARY.md
@.planning/phases/02-core-homeowner-page/02-02-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/types.ts:
```typescript
export interface HomeownerRecord {
  // ... includes purchaseDate, purchasePrice, currentValue, etc.
}

export interface HomeownerMetrics {
  currentValue: number;
  equityGained: number;
  appreciationPercent: number;
  earningsPerWeek: number;
  earningsPerMonth: number;
  earningsPerDay: number;
  netProceeds: number;
  ownershipMonths: number;
  holdAndRent: ScenarioResult;
  moveUp: ScenarioResult;
  equityPlay: ScenarioResult;
  // ... other fields
}
```

From src/lib/format.ts:
```typescript
export function formatCurrency(value: number, decimals?: number): string;
export function formatPercent(value: number, decimals?: number): string;
```

From src/components/homeowner/SectionWrapper.tsx:
```typescript
// Alternating cream/white section backgrounds
export function SectionWrapper({ children, index }: { children: ReactNode; index: number }): JSX.Element;
```

From src/components/homeowner/HomeownerPage.tsx:
```typescript
// "use client" orchestrator receiving record + metrics as props
// Currently renders: HeroSection, PersonalNote, PurchaseStory, CurrentValue,
// EquityGained, EarningsReframe, AppreciationChart, RentVsOwn, NetProceeds
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create animation utilities, narrative voice, and milestone logic</name>
  <files>
    package.json,
    src/lib/hooks/useCountUp.ts,
    src/lib/hooks/useCountUp.test.ts,
    src/lib/narrative.ts,
    src/lib/narrative.test.ts,
    src/lib/milestones.ts,
    src/lib/milestones.test.ts,
    src/components/homeowner/AnimatedSection.tsx,
    src/components/homeowner/CountUpNumber.tsx,
    src/components/homeowner/MilestoneCallout.tsx
  </files>
  <behavior>
    - getNarrativeVoice(23) returns "recent"
    - getNarrativeVoice(24) returns "midterm"
    - getNarrativeVoice(83) returns "midterm"
    - getNarrativeVoice(84) returns "longterm"
    - getNarrativeVoice(0) returns "recent"
    - selectMilestones returns empty array for brand new owner (0 months, 0 equity)
    - selectMilestones returns 1-year anniversary for 12-month owner
    - selectMilestones returns equity threshold for owner with $100K equity
    - selectMilestones returns max 3 milestones even when more qualify
    - selectMilestones sorts by impressiveness (higher equity threshold beats lower)
    - useCountUp returns 0 when isInView is false
    - useCountUp returns target value when animation completes
  </behavior>
  <action>
    1. Run `npm install motion` to add the animation library.

    2. Create `src/lib/narrative.ts`:
       - Export type `NarrativeVoice = "recent" | "midterm" | "longterm"`
       - Export `getNarrativeVoice(ownershipMonths: number): NarrativeVoice` -- under 24 = recent, 24-83 = midterm, 84+ = longterm
       - Export copy map objects for each data section (equityCopy, earningsCopy, currentValueCopy, purchaseStoryCopy, rentVsOwnCopy, netProceedsCopy) keyed by NarrativeVoice
       - Each copy map has `intro` (canyon SB7 narrative opener) and `supporting` (charcoal/70 supporting text)
       - SB7rules: homeowner is hero, never use "just", every intro names a problem or promise
       - Recent voice: forward-looking, "where you're headed" tone
       - Mid-term voice: balanced past and future
       - Long-term voice: rich retrospective, "look what you've built"

    3. Create `src/lib/milestones.ts`:
       - Export interface `Milestone { type: string; label: string; section: string }`
       - Export `selectMilestones(ownershipMonths, purchaseDate, equityGained, appreciationPercent): Milestone[]`
       - Candidate milestones: purchase anniversary (whole years, 1+), equity thresholds ($50K/$100K/$150K/$200K/$250K/$500K), appreciation percentage (20%+)
       - Score by impressiveness, return top 2-3
       - Use formatCurrency from src/lib/format.ts for equity labels

    4. Create `src/lib/hooks/useCountUp.ts`:
       - Export `useCountUp(target: number, isInView: boolean, duration?: number): number`
       - Uses requestAnimationFrame with easeInQuad (t*t) easing -- starts slow, accelerates to final value
       - hasAnimated ref prevents replay
       - Returns 0 until isInView becomes true, then animates to target
       - Duration default 1500ms

    5. Create `src/components/homeowner/AnimatedSection.tsx`:
       - Import motion from "motion/react"
       - Props: `children`, `stagger?: boolean` (default false)
       - When stagger=true: uses parent variants with staggerChildren: 0.25, children use childVariants
       - When stagger=false: single motion.div with opacity 0->1, y 30->0, duration 0.6
       - Both use `viewport={{ once: true, margin: "-100px" }}`
       - Export `childVariants` for use in section components

    6. Create `src/components/homeowner/CountUpNumber.tsx`:
       - Props: `value: number`, `format: "currency" | "percent" | "none"`, `prefix?: string`, `suffix?: string`
       - Uses useInView from "motion/react" and useCountUp hook
       - Renders formatted animated number (uses formatCurrency/formatPercent from src/lib/format.ts)
       - Wraps in motion.div with childVariants for stagger compatibility

    7. Create `src/components/homeowner/MilestoneCallout.tsx`:
       - Props: `milestone: Milestone`
       - Small inline badge/tag rendered below the hero number in relevant sections
       - Gold background (#C9953E) with charcoal text, rounded pill shape, small text
       - Example: "10-year homeowner" or "$100,000+ in equity"

    8. Write tests first (TDD), then implement:
       - `src/lib/narrative.test.ts`: boundary tests for getNarrativeVoice at 0, 23, 24, 83, 84 months
       - `src/lib/milestones.test.ts`: empty results, single milestone, max 3, sorting by impressiveness
       - `src/lib/hooks/useCountUp.test.ts`: returns 0 when not in view, returns target when complete
  </action>
  <verify>
    <automated>npx vitest run src/lib/narrative.test.ts src/lib/milestones.test.ts src/lib/hooks/useCountUp.test.ts</automated>
  </verify>
  <done>
    - motion installed in package.json
    - getNarrativeVoice returns correct voice at all boundary values
    - selectMilestones returns 0-3 milestones sorted by impressiveness
    - useCountUp animates from 0 to target with ease-in
    - AnimatedSection, CountUpNumber, MilestoneCallout components exist and export correctly
    - All tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire animations, adaptive narrative, and milestones into existing sections</name>
  <files>
    src/components/homeowner/HomeownerPage.tsx,
    src/components/homeowner/PurchaseStory.tsx,
    src/components/homeowner/CurrentValue.tsx,
    src/components/homeowner/EquityGained.tsx,
    src/components/homeowner/EarningsReframe.tsx,
    src/components/homeowner/RentVsOwn.tsx,
    src/components/homeowner/NetProceeds.tsx,
    src/components/homeowner/AppreciationChart.tsx,
    src/components/homeowner/PersonalNote.tsx,
    src/components/homeowner/HeroSection.tsx
  </files>
  <action>
    1. Update `HomeownerPage.tsx`:
       - Import getNarrativeVoice, selectMilestones, AnimatedSection
       - Compute `voice = getNarrativeVoice(metrics.ownershipMonths)` at top
       - Compute `milestones = selectMilestones(metrics.ownershipMonths, record.purchaseDate, metrics.equityGained, metrics.appreciationPercent)`
       - Pass `voice` prop to all data section components
       - Pass relevant milestone to each section (find milestone where milestone.section matches)
       - Wrap HeroSection in AnimatedSection (stagger=false) -- or leave unwrapped if hero should be immediately visible (hero is above the fold, no scroll trigger needed)
       - Wrap PersonalNote in AnimatedSection (stagger=false)

    2. Update key data sections with staggered animation (EquityGained, EarningsReframe, NetProceeds, CurrentValue):
       - Wrap section content in `<AnimatedSection stagger>`
       - Wrap SB7 intro paragraph in `<motion.p variants={childVariants}>`
       - Replace static hero number with `<CountUpNumber value={...} format="currency" />`  wrapped in motion.div with childVariants
       - Wrap supporting copy in `<motion.p variants={childVariants}>`
       - Accept `voice: NarrativeVoice` prop, use corresponding copy map for SB7 intro and supporting text
       - Accept optional `milestone: Milestone` prop, render MilestoneCallout below hero number when present
       - Import motion from "motion/react" and childVariants from AnimatedSection

    3. Update quieter sections with unit animation (PurchaseStory, RentVsOwn):
       - Wrap in `<AnimatedSection stagger={false}>`
       - Accept `voice: NarrativeVoice` prop, use copy map for SB7 intro text
       - PurchaseStory: narrative copy adapts (recent: "Your story started here", longterm: "Years ago, you made a decision that changed everything")
       - RentVsOwn: narrative framing adapts but villain/triumph card structure stays the same

    4. Update PersonalNote and HeroSection:
       - PersonalNote: wrap in AnimatedSection (stagger=false), no voice adaptation (personal notes are Josh's words)
       - HeroSection: NO animation wrapper (above the fold, should render immediately)

    5. Update AppreciationChart:
       - Add useInView from "motion/react" with `{ once: true, margin: "-100px" }`
       - Control `isAnimationActive={isInView}` on the Recharts Area component
       - Set `animationDuration={1500}` and `animationEasing="ease-in-out"` for cinematic draw
       - Wrap in AnimatedSection (stagger=false) for the fade-in, chart draws itself inside

    6. Ensure all animations only play once (viewport={{ once: true }}). No scroll-back replay.
    7. Keep mobile performance: only animate opacity and transform, avoid layout-triggering properties.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>
    - All data sections animate on scroll with staggered element entrances for key sections
    - Hero numbers count up with ease-in acceleration
    - Quieter sections animate as a single unit
    - Narrative text adapts based on ownership duration (visible in different copy for different ownershipMonths values)
    - Milestone callouts appear inline in relevant sections
    - Appreciation chart draws itself when scrolled into view
    - Animations play only once
    - Build succeeds with no TypeScript errors
    - All existing tests still pass
  </done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (narrative, milestones, countUp, plus all existing tests)
- `npm run build` -- no TypeScript errors, production build succeeds
- Visit /h/[slug] and scroll -- sections reveal theatrically, numbers count up, chart draws
- Change purchaseDate to test narrative voice: recent buyer (<2yr) sees forward-looking copy, long-term (7+yr) sees retrospective copy
</verification>

<success_criteria>
- Scrolling the homeowner page triggers cinematic section reveals with staggered element entrances
- Hero numbers (equity, earnings, net proceeds, current value) count up from 0 with ease-in acceleration
- SB7 narrative intro text and supporting copy differ based on ownership duration
- 2-3 milestone callouts appear inline in relevant sections based on homeowner data
- Appreciation chart draws itself on scroll
- All animations play exactly once
- Build passes, all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-narrative-experience/03-01-SUMMARY.md`
</output>
