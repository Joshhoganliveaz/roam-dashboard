---
phase: 03-narrative-experience
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/homeowner/ScenarioCards.tsx
  - src/components/homeowner/ScenarioCard.tsx
  - src/components/homeowner/ScenarioCards.test.tsx
  - src/components/homeowner/CTASection.tsx
  - src/components/homeowner/CTASection.test.tsx
  - src/components/homeowner/HomeownerPage.tsx
  - public/team/josh.jpg
  - public/team/jacqui.jpg
autonomous: false
requirements: [SCEN-01, SCEN-02, SCEN-03, NARR-08]

must_haves:
  truths:
    - "Three scenario cards are visible after Net Proceeds: Hold & Rent, Move-Up, Equity Play"
    - "Each card shows a hero metric and one-line implication when collapsed"
    - "Tapping a card expands it to show 2-3 supporting details and a cliff question"
    - "Scenario cards use pre-computed data from HomeownerMetrics -- no new calculations"
    - "The page ends with a branded CTA section with Josh + Jacqui photos and an sms: text link"
    - "CTA feels like a natural narrative conclusion, not a sales pitch"
    - "Scenario cards stagger-animate in on scroll"
  artifacts:
    - path: "src/components/homeowner/ScenarioCards.tsx"
      provides: "Card cluster section with header and three scenario cards"
      exports: ["ScenarioCards"]
    - path: "src/components/homeowner/ScenarioCard.tsx"
      provides: "Individual expandable scenario card"
      exports: ["ScenarioCard"]
    - path: "src/components/homeowner/ScenarioCards.test.tsx"
      provides: "Render tests for all three cards"
    - path: "src/components/homeowner/CTASection.tsx"
      provides: "Branded closing section with team CTA"
      exports: ["CTASection"]
    - path: "src/components/homeowner/CTASection.test.tsx"
      provides: "Render test for sms link presence"
  key_links:
    - from: "src/components/homeowner/ScenarioCards.tsx"
      to: "src/lib/types.ts"
      via: "HomeownerMetrics.holdAndRent, .moveUp, .equityPlay"
      pattern: "metrics\\.(holdAndRent|moveUp|equityPlay)"
    - from: "src/components/homeowner/ScenarioCard.tsx"
      to: "motion/react"
      via: "AnimatePresence for expand/collapse"
      pattern: "AnimatePresence"
    - from: "src/components/homeowner/CTASection.tsx"
      to: "sms:"
      via: "href with sms: protocol to Josh's number"
      pattern: "sms:"
    - from: "src/components/homeowner/HomeownerPage.tsx"
      to: "src/components/homeowner/ScenarioCards.tsx"
      via: "Rendered after NetProceeds with metrics prop"
      pattern: "ScenarioCards"
---

<objective>
Build the future scenario card cluster and branded CTA closing section, then wire them into the HomeownerPage as the final two sections. Scenario cards use tap-to-expand interaction with pre-computed data from HomeownerMetrics, each stopping at a deliberate information cliff. The CTA provides a warm narrative sign-off with team photos and a single sms: text link.

Purpose: Complete the narrative arc -- after showing what the homeowner has built, reveal what they could do next, then close with a warm invitation to talk to Josh.
Output: Three animated scenario cards and a branded CTA section completing the homeowner page experience.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-narrative-experience/03-CONTEXT.md
@.planning/phases/03-narrative-experience/03-RESEARCH.md
@.planning/phases/03-narrative-experience/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/components/homeowner/AnimatedSection.tsx:
```typescript
export function AnimatedSection({ children, stagger }: { children: ReactNode; stagger?: boolean }): JSX.Element;
export const childVariants: Variants;
```

From src/lib/narrative.ts:
```typescript
export type NarrativeVoice = "recent" | "midterm" | "longterm";
export function getNarrativeVoice(ownershipMonths: number): NarrativeVoice;
```

From src/lib/types.ts (existing):
```typescript
export interface ScenarioResult {
  keyMetric: number;
  description: string;
  details: Record<string, number>;
}

export interface HomeownerMetrics {
  // ... includes:
  holdAndRent: ScenarioResult;
  moveUp: ScenarioResult;
  equityPlay: ScenarioResult;
  ownershipMonths: number;
  netProceeds: number;
  equityGained: number;
}
```

From src/lib/format.ts:
```typescript
export function formatCurrency(value: number, decimals?: number): string;
export function formatPercent(value: number, decimals?: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build ScenarioCards, ScenarioCard, and CTASection components</name>
  <files>
    src/components/homeowner/ScenarioCard.tsx,
    src/components/homeowner/ScenarioCards.tsx,
    src/components/homeowner/ScenarioCards.test.tsx,
    src/components/homeowner/CTASection.tsx,
    src/components/homeowner/CTASection.test.tsx,
    public/team/josh.jpg,
    public/team/jacqui.jpg
  </files>
  <behavior>
    - ScenarioCards renders three cards with titles "Hold & Rent", "Move Up", "Equity Play"
    - Each card shows a hero metric value when collapsed
    - Each card shows an implication line when collapsed
    - Expanded card shows 2-3 detail rows with label and value
    - Expanded card shows an italic cliff question
    - CTASection renders an sms: link with Josh's phone number (480) 369-8280
    - CTASection renders team photo images
    - CTASection contains warm narrative text (not sales-pitch language)
  </behavior>
  <action>
    1. Create `src/components/homeowner/ScenarioCard.tsx`:
       - Props: `title`, `heroMetric: string`, `implication: string`, `details: { label: string; value: string }[]`, `cliffQuestion: string`
       - Uses useState for expanded toggle
       - Collapsed state: title, hero metric (olive, text-2xl, font-bold), one-line implication (charcoal/60)
       - Expanded state: animated with motion AnimatePresence, shows detail rows (flex justify-between) + italic canyon cliff question
       - Expand animation: height 0->auto, opacity 0->1 via motion.div
       - Whole card is a button for tap interaction
       - Styling: white bg, rounded-xl, p-6, shadow-sm, border charcoal/10
       - Wrap in motion.div with childVariants for stagger compatibility from AnimatedSection

    2. Create `src/components/homeowner/ScenarioCards.tsx`:
       - Props: `metrics: HomeownerMetrics`, `voice: NarrativeVoice`
       - Section header: "Where could you go from here?" with voice-appropriate SB7 intro
       - Recent voice: "Your home is already working for you. Here is what the future could hold."
       - Mid-term voice: "You have built real wealth. Now the question becomes -- what do you do with it?"
       - Long-term voice: "After everything you have built, the possibilities are remarkable."
       - Maps metrics.holdAndRent, metrics.moveUp, metrics.equityPlay to ScenarioCard props:
         - Hold & Rent: heroMetric = monthly rent formatted, implication based on cash flow positive/negative, details: rental income, mortgage, cash flow. Cliff: "What would my actual numbers look like?"
         - Move Up: heroMetric = purchasing power formatted, implication about what equity unlocks. Details: net proceeds, 20% down payment, purchasing power. Cliff: "What could I actually get in the areas I want?"
         - Equity Play: heroMetric = HELOC amount formatted, implication about investment leverage. Details: available equity, HELOC at 80% LTV, investment leverage. Cliff: "Is this actually a good move for my situation?"
       - Wrap card cluster in AnimatedSection with stagger=true
       - Use SectionWrapper for consistent background alternation (pass appropriate index)
       - Use formatCurrency from src/lib/format.ts for all values
       - Cards are NOT calculators -- present pre-computed data only

    3. Create `src/components/homeowner/CTASection.tsx`:
       - Branded closing section with canyon or olive background (set apart from data sections)
       - Josh + Jacqui placeholder photos side by side (Next.js Image from /team/josh.jpg and /team/jacqui.jpg)
       - Photos: circular crop, side by side, medium size (~80px each)
       - Warm narrative sign-off: "Ready to explore your options? Let's talk." (or similar warm SB7-aligned copy -- NOT "Contact us today!" or "Schedule now!")
       - Subtext: "Josh Hogan & Jacqui Hogan | Live AZ Co"
       - Single action: `<a href="sms:+14803698280">Text us</a>` styled as a button (cream text on charcoal bg, rounded-full, px-8 py-3)
       - Wrap in AnimatedSection (stagger=false) for scroll reveal
       - SB7 rules: feels like a natural conclusion, NOT a sales pitch. The homeowner just saw their story -- this is the "what's next" moment

    4. Add placeholder team photos:
       - Create `public/team/` directory
       - Add simple placeholder images (can be solid-color circles or minimal SVGs) -- Josh will replace with real photos
       - Use Next.js `<Image>` with width/height for optimization

    5. Write tests first (TDD):
       - `src/components/homeowner/ScenarioCards.test.tsx`: renders three cards with correct titles, each card shows hero metric, tests with mock HomeownerMetrics
       - `src/components/homeowner/CTASection.test.tsx`: renders sms: link with correct phone number, renders team name text
       - Note: Motion/AnimatePresence may need mocking in tests. If jsdom issues arise with motion, test the data mapping logic and rendered text content rather than animation behavior.
  </action>
  <verify>
    <automated>npx vitest run src/components/homeowner/ScenarioCards.test.tsx src/components/homeowner/CTASection.test.tsx</automated>
  </verify>
  <done>
    - ScenarioCard renders collapsed/expanded states with smooth animation
    - ScenarioCards maps all three scenarios from HomeownerMetrics correctly
    - Each card stops at a deliberate information cliff
    - CTASection renders warm narrative CTA with sms: link to (480) 369-8280
    - Team photo placeholders in public/team/
    - All component tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire ScenarioCards and CTASection into HomeownerPage</name>
  <files>
    src/components/homeowner/HomeownerPage.tsx
  </files>
  <action>
    1. Import ScenarioCards and CTASection into HomeownerPage.tsx

    2. Add ScenarioCards after the NetProceeds section:
       - Pass `metrics={metrics}` and `voice={voice}` props
       - Position: after NetProceeds, before CTASection
       - Wrap in SectionWrapper with appropriate index for background alternation

    3. Add CTASection as the final element on the page:
       - No SectionWrapper needed (CTASection has its own branded background)
       - This is the last thing the homeowner sees

    4. Verify section order in HomeownerPage:
       1. HeroSection (no animation -- above fold)
       2. PersonalNote (unit animation)
       3. PurchaseStory (unit animation)
       4. CurrentValue (staggered animation)
       5. EquityGained (staggered animation)
       6. EarningsReframe (staggered animation)
       7. AppreciationChart (chart draws on scroll)
       8. RentVsOwn (unit animation)
       9. NetProceeds (staggered animation)
       10. ScenarioCards (staggered card cluster)
       11. CTASection (unit animation, branded background)

    5. Ensure no TypeScript errors, all props correctly typed.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>
    - HomeownerPage renders all 11 sections in correct narrative order
    - ScenarioCards appear after NetProceeds with three expandable cards
    - CTASection closes the page with warm CTA and sms link
    - Build succeeds with no TypeScript errors
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of complete narrative experience</name>
  <files>n/a</files>
  <action>
    Human verifies the complete Phase 3 narrative experience including scroll-triggered animations on all sections, hero number countups, adaptive narrative voice based on ownership duration, milestone callouts, three future scenario cards with tap-to-expand, and branded CTA closing section with sms link.
  </action>
  <verify>User confirms all visual and interactive elements work correctly on desktop and mobile viewport</verify>
  <done>User types "approved" confirming the narrative experience meets expectations</done>
  <what-built>Complete Phase 3 narrative experience: scroll-triggered animations on all sections, hero number countups, adaptive narrative voice based on ownership duration, milestone callouts, three future scenario cards with tap-to-expand, and branded CTA closing section with sms link</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit a homeowner page at http://localhost:3000/h/[slug]
    2. Scroll down slowly -- verify sections fade/slide in theatrically as you scroll
    3. Watch hero numbers (equity, earnings, current value, net proceeds) -- they should count up from 0 with acceleration
    4. Verify the appreciation chart draws itself when scrolled into view
    5. Scroll back up -- animations should NOT replay
    6. Check narrative text matches ownership duration (if homeowner has been owner 5+ years, text should be retrospective; if recent, forward-looking)
    7. Look for 1-3 gold milestone badges inline near relevant hero numbers
    8. After Net Proceeds, verify three scenario cards appear: Hold & Rent, Move-Up, Equity Play
    9. Tap each card -- it should expand with supporting details and end with an italic question
    10. Scroll to bottom -- verify branded CTA section with team photos and "Text us" sms link
    11. Test on mobile viewport (Chrome DevTools, 375px width) -- animations should be smooth, not janky
    12. Click "Text us" -- should open SMS to (480) 369-8280
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (scenario cards, CTA, plus all existing and Plan 01 tests)
- `npm run build` -- production build succeeds with no errors
- Visual verification on desktop and mobile viewport
- Complete narrative arc: hero -> data sections with animation -> scenarios -> CTA
</verification>

<success_criteria>
- Three scenario cards visible after Net Proceeds, each showing pre-computed data with tap-to-expand
- Each expanded card ends at an information cliff (question that requires Josh)
- Branded CTA section closes the page with team feel and sms: text link
- Scenario cards stagger-animate on scroll
- CTA reads as natural conclusion, not sales pitch
- All tests pass, build succeeds
- User has visually verified the complete narrative experience
</success_criteria>

<output>
After completion, create `.planning/phases/03-narrative-experience/03-02-SUMMARY.md`
</output>
