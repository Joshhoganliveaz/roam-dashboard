---
phase: 02.1-financial-data-audit-edge-cases
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/sp500-data.json
  - src/lib/sp500-lookup.ts
  - src/lib/sp500-lookup.test.ts
  - src/lib/calculations.ts
  - src/lib/defaults.ts
autonomous: true
requirements: [DATA-06, SP5-01, SP5-02, SP5-03, SP5-04]

must_haves:
  truths:
    - "S&P 500 comparison uses real monthly closing prices, not a fixed 10.5% average"
    - "getSP500Price returns the correct closing price for a known month"
    - "getSP500Price falls back to nearest available month when exact match is missing"
    - "calcRealSP500Return computes actual growth rate from real data"
    - "calcRealSP500Return falls back to historical average when data is unavailable"
    - "calcSP500Comparison accepts optional sp500ActualReturn for real data"
  artifacts:
    - path: "src/lib/sp500-data.json"
      provides: "Monthly S&P 500 closing prices from ~1993 to 2026"
      min_lines: 360
    - path: "src/lib/sp500-lookup.ts"
      provides: "S&P 500 price lookup and return calculation"
      exports: ["getSP500Price", "calcRealSP500Return"]
    - path: "src/lib/sp500-lookup.test.ts"
      provides: "Tests for S&P 500 lookup and return calculation"
      min_lines: 50
    - path: "src/lib/calculations.ts"
      provides: "Updated calcSP500Comparison with optional real data parameter"
      contains: "sp500ActualReturn"
  key_links:
    - from: "src/lib/sp500-lookup.ts"
      to: "src/lib/sp500-data.json"
      via: "JSON import"
      pattern: "import.*sp500-data"
    - from: "src/lib/calculations.ts"
      to: "src/lib/sp500-lookup.ts"
      via: "computeHomeownerMetrics calls calcRealSP500Return"
      pattern: "calcRealSP500Return"
---

<objective>
Replace the fixed 10.5% S&P 500 average with real historical monthly closing price data.

Purpose: Make the S&P 500 comparison credible and defensible -- "Your home gained $80K vs $27K if you'd invested in the S&P 500" using actual market data for the exact ownership period.
Output: Static S&P 500 JSON data file, lookup module with tests, updated calcSP500Comparison signature.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-financial-data-audit-edge-cases/02.1-CONTEXT.md
@.planning/phases/02.1-financial-data-audit-edge-cases/02.1-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/calculations.ts:
```typescript
export function calcSP500Comparison(
  downPayment: number,
  months: number,
  homeAppreciationDollars: number
): { homeReturn: number; sp500Return: number; difference: number }
```

From src/lib/defaults.ts:
```typescript
export const DEFAULTS = {
  sp500AnnualReturn: 0.105,  // TO BE KEPT as fallback
  // ... other defaults
} as const;
```

From src/lib/types.ts:
```typescript
export interface HomeownerMetrics {
  sp500Comparison: {
    homeReturn: number;
    sp500Return: number;
    difference: number;
  };
  // ...
}
```

From src/lib/calculations.ts (computeHomeownerMetrics SP500 call site):
```typescript
// Currently calls:
const sp500Comparison = calcSP500Comparison(downPayment, months, homeAppreciationDollars);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create S&P 500 data file and lookup module with tests</name>
  <files>src/lib/sp500-data.json, src/lib/sp500-lookup.ts, src/lib/sp500-lookup.test.ts</files>
  <behavior>
    - getSP500Price("2020-01") returns a number (the Jan 2020 closing price)
    - getSP500Price("1993-02") returns a number (early data point)
    - getSP500Price("2099-12") returns null (future date, no data)
    - getSP500Price for a month between two data points falls back to nearest earlier month
    - calcRealSP500Return with valid dates returns { sp500Return: number, isRealData: true }
    - calcRealSP500Return with a date before data range returns { sp500Return: number, isRealData: false } (fallback)
    - calcRealSP500Return with downPayment=100000 and ~10 years of real data returns a plausible dollar gain
  </behavior>
  <action>
    1. Create src/lib/sp500-data.json: Array of `{ "date": "YYYY-MM", "close": number }` objects with monthly S&P 500 closing prices. Start from January 1993 through February 2026. Use real historical monthly closing prices. This file will be ~15KB. Include a comment-style first entry or rely on the module documentation for update instructions.
       - Key reference points to verify accuracy: Jan 2000 ~1394, Jan 2009 ~903, Jan 2020 ~3226, Jan 2024 ~4770
       - Source the data from publicly available S&P 500 historical monthly closing prices
    2. Create src/lib/sp500-lookup.ts with:
       - `SP500DataPoint` interface: `{ date: string; close: number }`
       - `getSP500Price(yearMonth: string): number | null` -- exact match first, then nearest earlier month, null if before all data
       - `calcRealSP500Return(downPayment: number, purchaseDate: Date, currentDate?: Date): { sp500Return: number; isRealData: boolean }` -- calculates actual return from real price data, falls back to DEFAULTS.sp500AnnualReturn if data missing
       - Document the update process in a JSDoc comment at module top
    3. Create src/lib/sp500-lookup.test.ts with tests for all behavior cases above.
  </action>
  <verify>
    <automated>cd /Users/joshuahogan/Projects/homeowner-journey-map && npx vitest run src/lib/sp500-lookup.test.ts</automated>
  </verify>
  <done>S&P 500 data file with 390+ monthly entries, lookup module returning real prices, all tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire real S&P data into calcSP500Comparison and computeHomeownerMetrics</name>
  <files>src/lib/calculations.ts, src/lib/types.ts</files>
  <action>
    1. Update the `sp500Comparison` type in HomeownerMetrics (types.ts) to add `isRealData: boolean` field.
    2. Update `calcSP500Comparison` signature to accept an optional `sp500ActualReturn?: number` parameter:
       - If sp500ActualReturn is provided, use it directly instead of computing from DEFAULTS.sp500AnnualReturn
       - Return object gains `isRealData: boolean` field
       - Existing tests must still pass (the parameter is optional, backward compatible)
    3. Update `computeHomeownerMetrics` to:
       - Import and call `calcRealSP500Return` from sp500-lookup.ts
       - Pass the real sp500Return to `calcSP500Comparison` as the new optional parameter
       - This wires real data through the entire pipeline
    4. Run full test suite to confirm backward compatibility. Existing calcSP500Comparison tests should pass since the new parameter is optional.
  </action>
  <verify>
    <automated>cd /Users/joshuahogan/Projects/homeowner-journey-map && npx vitest run</automated>
  </verify>
  <done>calcSP500Comparison uses real S&P data when available, falls back to 10.5% average, isRealData flag indicates which path was taken, all existing tests pass</done>
</task>

</tasks>

<verification>
```bash
cd /Users/joshuahogan/Projects/homeowner-journey-map && npx vitest run
```
Full test suite passes. S&P 500 lookup returns real prices. calcSP500Comparison uses actual data.
</verification>

<success_criteria>
- sp500-data.json exists with 390+ monthly entries from 1993-2026
- getSP500Price returns correct prices for known months
- calcRealSP500Return computes real returns and flags isRealData=true
- computeHomeownerMetrics flows real S&P data through the pipeline
- All tests pass including existing calcSP500Comparison tests (backward compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-financial-data-audit-edge-cases/02.1-02-SUMMARY.md`
</output>
