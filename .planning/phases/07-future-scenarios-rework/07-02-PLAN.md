---
phase: 07-future-scenarios-rework
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/homeowner/CTASection.tsx
  - src/components/homeowner/AppreciationChart.tsx
autonomous: true
requirements: [PAGE-03, PAGE-05]

must_haves:
  truths:
    - "Bottom CTA shows Josh & Jacqui contact info with photo and 'Let's talk about your options'"
    - "Cromford chart date labels are readable on mobile without overlapping"
  artifacts:
    - path: "src/components/homeowner/CTASection.tsx"
      provides: "Simple contact CTA with Josh & Jacqui"
      contains: "Let's talk about your options"
    - path: "src/components/homeowner/AppreciationChart.tsx"
      provides: "Mobile-friendly date label spacing"
      contains: "angle"
  key_links:
    - from: "src/components/homeowner/CTASection.tsx"
      to: "public/team/"
      via: "Image src for team photos"
      pattern: "team/josh|team/jacqui"
---

<objective>
Replace the CTA section with a simple Josh & Jacqui contact closing, and fix the Cromford chart date label spacing on mobile.

Purpose: The CTA should feel like a warm close, not a sales pitch -- Josh & Jacqui's photos, contact info, and "Let's talk about your options." Individual scenario cards will have their own CTAs in Phase 9, so the bottom CTA is a simple sign-off. The Cromford chart fix ensures date labels are readable on all screen sizes.

Output: Updated CTASection component and mobile-responsive AppreciationChart date labels.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-future-scenarios-rework/07-CONTEXT.md

<interfaces>
<!-- Current CTASection.tsx -- will be replaced -->
From src/components/homeowner/CTASection.tsx:
```typescript
// Current: "Ready to explore your options?" heading, team photos, "Text us" button
// Already has Josh/Jacqui photos at /team/josh.jpg and /team/jacqui.jpg
export function CTASection() { ... }
```

From src/components/homeowner/AppreciationChart.tsx:
```typescript
// XAxis config (lines 217-223):
<XAxis
  dataKey="date"
  tick={{ fontSize: 12 }}
  interval={tickInterval}  // Math.max(1, Math.floor(data.length / 5) - 1)
  tickLine={false}
  axisLine={{ stroke: "#2C2C2C", strokeOpacity: 0.15 }}
/>
// Dates are formatted as "MMM yyyy" (e.g., "Jan 2024")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CTA with simple Josh & Jacqui contact section</name>
  <files>src/components/homeowner/CTASection.tsx</files>
  <action>
Rewrite the CTASection component to be a simple, warm closing:

- Keep the charcoal background (`bg-charcoal`) and AnimatedSection wrapper
- Keep the Josh and Jacqui circular photo layout (already using `/team/josh.jpg` and `/team/jacqui.jpg`)
- Change the heading from "Ready to explore your options?" to "Let's talk about your options"
- Replace the supporting copy: instead of "Your home has a story worth telling -- and a future worth planning." use something warmer and simpler: "Whether you're curious, planning ahead, or ready to make a move -- we're here."
- Keep "Josh Hogan & Jacqui Hogan | Live AZ Co" name line
- Add contact details below the names:
  - Phone: (480) 369-8280 as a clickable `tel:` link
  - Text: "Send us a text" as a clickable `sms:+14803698280` link
  - Keep both contact options visible (not just "Text us" button)
- Style contact links as simple text links (not big buttons) -- `text-cream/80 hover:text-cream underline` style
- Remove the large "Text us" rounded button -- replace with the inline contact links above
- Keep the overall feel: calm, approachable, not pushy. This is SB7 guide energy.
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- CTA section shows "Let's talk about your options" heading
- Josh & Jacqui photos displayed
- Phone number and text links both visible and clickable
- No large call-to-action button -- simple, warm contact info
- Maintains charcoal background and branded styling
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix Cromford chart date label spacing on mobile</name>
  <files>src/components/homeowner/AppreciationChart.tsx</files>
  <action>
Fix the XAxis date label crowding on mobile screens:

1. **Increase tick interval on mobile:** The current `tickInterval` is `Math.max(1, Math.floor(data.length / 5) - 1)`. This shows ~5 labels regardless of screen width, which crowds on mobile. Change the approach:
   - Use a responsive tick interval: for data arrays with many points (10+), show fewer labels. A good formula: `Math.max(1, Math.floor(data.length / 4) - 1)` gives ~4 labels, reducing crowding.
   - Alternatively, use Recharts' `tick` render prop to detect available width.

2. **Rotate labels on small screens:** Add an `angle` prop to the XAxis `tick` configuration. Set `angle={-45}` with `textAnchor="end"` for the tick text. This prevents horizontal overlap. Increase the bottom margin of the AreaChart from `0` to `40` to accommodate rotated labels: `margin={{ top: 30, right: 80, left: 10, bottom: 40 }}`.

3. **Shorten date format on mobile:** Instead of "MMM yyyy" (e.g., "Jan 2024"), consider using a custom tick renderer that shows just the year when labels would overlap. However, the simpler fix is the rotation + interval approach above -- implement that first.

4. **Reduce font size on mobile:** Change `tick={{ fontSize: 12 }}` to `tick={{ fontSize: 11 }}` for XAxis to give more breathing room.

The key fix is the combination of: increased interval (fewer labels) + rotated text (no overlap) + bottom margin (room for rotated text). This addresses PAGE-05 without changing any data or chart behavior.
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
- Cromford chart date labels do not overlap on mobile viewports (375px width)
- Labels are rotated or spaced to remain readable
- Chart bottom margin accommodates rotated labels without cutting them off
- Chart still looks good on desktop (labels not overly sparse)
  </done>
</task>

</tasks>

<verification>
- `cd ~/Projects/homeowner-journey-map && npx tsc --noEmit` -- zero type errors
- `cd ~/Projects/homeowner-journey-map && npx vitest run` -- all tests pass (including existing CTASection tests)
- Visual: CTA shows "Let's talk about your options" with Josh/Jacqui photos and contact info
- Visual: Cromford chart date labels readable on mobile (Chrome DevTools, iPhone SE 375px)
</verification>

<success_criteria>
- PAGE-03: Simple CTA at bottom with Josh & Jacqui contact info, photo, and "Let's talk about your options"
- PAGE-05: Cromford chart date labels readable on mobile with no overlapping text
- Existing CTASection tests pass (update if needed to match new heading text)
</success_criteria>

<output>
After completion, create `.planning/phases/07-future-scenarios-rework/07-02-SUMMARY.md`
</output>
