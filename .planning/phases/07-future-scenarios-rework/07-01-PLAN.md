---
phase: 07-future-scenarios-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/homeowner/HomeownerPage.tsx
  - src/components/homeowner/HeroSection.tsx
  - src/components/homeowner/ConditionCompLinks.tsx
autonomous: false
requirements: [PAGE-01, PAGE-02, PAGE-04, PAGE-06]

must_haves:
  truths:
    - "Weekly paycheck ('Your home earned $X this week') is the first compelling number visible on page load"
    - "Scenario cards appear before appreciation chart, investment comparison, and mortgage details"
    - "No external links take the homeowner off the page"
    - "Net proceeds does not appear as a standalone section"
  artifacts:
    - path: "src/components/homeowner/HeroSection.tsx"
      provides: "Hero with weekly paycheck hook number"
      contains: "earningsPerWeek"
    - path: "src/components/homeowner/HomeownerPage.tsx"
      provides: "Reordered page sections"
      contains: "ScenarioCards"
  key_links:
    - from: "src/components/homeowner/HomeownerPage.tsx"
      to: "src/components/homeowner/HeroSection.tsx"
      via: "earningsPerWeek prop"
      pattern: "earningsPerWeek.*metrics\\.earningsPerWeek"
---

<objective>
Move the weekly paycheck hook to the hero position, reorder page sections so scenarios come before data/proof sections, remove the standalone net proceeds section, and remove the "see a similar home" external link.

Purpose: The page should hook homeowners immediately with "Your home earned $X this week" and show opportunities (scenarios) before proof (charts, comparisons). Removing net proceeds from standalone avoids a premature selling pitch -- it belongs in the Sell & Move Up scenario card (Phase 9). Removing external links keeps homeowners on the page.

Output: Restructured HomeownerPage with new section order and updated HeroSection displaying the weekly paycheck hook.
</objective>

<execution_context>
@/Users/joshuahogan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joshuahogan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-future-scenarios-rework/07-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/components/homeowner/HeroSection.tsx:
```typescript
interface HeroSectionProps {
  clientName: string;
  address: string;
  city: string;
  state: string;
  homePhotoUrl: string | null;
}
```

From src/components/homeowner/HomeownerPage.tsx:
```typescript
interface HomeownerPageProps {
  record: HomeownerRecord;
  conditionVariants: ConditionVariants;
  conditionValues: { original: number; updated: number; remodeled: number };
  defaultCondition: Condition;
  cromfordData: CromfordData | null;
  currentMarketRate: number;
}
```

From src/components/homeowner/EarningsReframe.tsx:
```typescript
interface EarningsReframeProps {
  earningsPerWeek: number;
  earningsPerDay: number;
  earningsPerMonth: number;
  voice: NarrativeVoice;
  cascadeDelay?: number;
}
```

Current section order in HomeownerPage.tsx (lines 102-209):
1. HeroSection (name, address, photo)
2. PersonalNote
3. PurchaseStory
4. CurrentValue (+ condition picker)
5. EquityGained
6. InvestmentComparison
7. EarningsReframe
8. AppreciationChart
9. ConditionCompLinks (external "see a similar home" link)
10. NetProceeds
11. ScenarioCards
12. CTASection
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add weekly paycheck to HeroSection and reorder page sections</name>
  <files>src/components/homeowner/HeroSection.tsx, src/components/homeowner/HomeownerPage.tsx</files>
  <action>
**HeroSection.tsx changes:**
- Add new props: `earningsPerWeek: number` and `isNegative?: boolean` (for underwater homes)
- Below the existing name/address overlay at the bottom of the hero, add the weekly paycheck hook as a prominent callout:
  - When earningsPerWeek > 0: Display "Your home earned you" above a large formatted number "$X" with "/week" suffix, using formatCurrency from `@/lib/format`
  - When earningsPerWeek <= 0: Display "Your home's value is shifting" with a smaller supportive line like "Every market has seasons." (no negative dollar amount in the hero -- keep it dignified)
  - Style: The paycheck number should be the visual hero -- large, bold, white text on the dark scrim. Use `font-heading text-3xl md:text-5xl font-bold text-white` for the number. The "Your home earned you" label should be `font-body text-base text-white/80`
  - Position the paycheck block between the name/address and the bottom edge of the hero, with enough spacing to feel like the headline number

**HomeownerPage.tsx changes:**
- Pass `earningsPerWeek={metrics.earningsPerWeek}` to HeroSection
- Reorder sections to match the locked decision from CONTEXT.md:
  1. HeroSection (with weekly paycheck hook) -- PAGE-01
  2. PersonalNote
  3. CurrentValue (+ condition picker) + EquityGained -- home value and equity together
  4. ScenarioCards -- moved UP from position 11 to position 4 -- PAGE-02
  5. AppreciationChart
  6. InvestmentComparison
  7. EarningsReframe (detailed breakdown with per-month/per-day -- now supporting data, not the hook)
  8. PurchaseStory (mortgage details / purchase context -- moved down as supporting data)
  9. CTASection
- Remove the NetProceeds component render entirely (do NOT delete the file -- Phase 9 may reuse the breakdown logic) -- PAGE-06
- Remove the ConditionCompLinks render and its conditional block -- PAGE-04
- Remove the imports for NetProceeds and ConditionCompLinks
- Update cascade delays: Since sections are reordered, adjust cascadeDelay values to maintain the visual cascade effect. Start from 0 at CurrentValue and increment by 500ms down through the remaining animated sections.
- Keep EarningsReframe in the page but move it below InvestmentComparison -- it serves as detailed supporting data now that the hero has the headline number.
  </action>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
- HeroSection displays the weekly paycheck number ("Your home earned you $X/week") as the hero hook visible on page load
- Page sections are reordered: hero+hook -> note -> value+equity -> scenarios -> chart -> comparison -> earnings detail -> purchase story -> CTA
- NetProceeds component is not rendered (file preserved for Phase 9)
- ConditionCompLinks component is not rendered (no external links on page)
- TypeScript compiles without errors
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify hero hook and page flow</name>
  <files>none</files>
  <action>Human verifies the hero hook and page section reorder visually in the browser.</action>
  <what-built>Weekly paycheck hero hook and page section reorder. The homeowner page now leads with "Your home earned you $X/week" in the hero, shows scenarios before data sections, and has no external links or standalone net proceeds.</what-built>
  <how-to-verify>
    1. Run `cd ~/Projects/homeowner-journey-map && npm run dev`
    2. Visit a homeowner page (e.g., http://localhost:3000/h/[any-slug])
    3. Verify the hero section shows the weekly paycheck number prominently below the name/address
    4. Scroll down and verify section order: personal note -> home value + equity -> scenario cards -> chart -> investment comparison -> earnings detail -> purchase story -> CTA
    5. Verify there is no "See a similar home" link anywhere on the page
    6. Verify there is no standalone "If you sold today" net proceeds section
    7. Check mobile viewport (Chrome DevTools, iPhone SE or similar) -- hero paycheck number should be readable and well-sized
  </how-to-verify>
  <verify>
    <automated>cd ~/Projects/homeowner-journey-map && npx vitest run 2>&1 | tail -5</automated>
  </verify>
  <done>User confirms hero hook and page flow look correct on desktop and mobile</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cd ~/Projects/homeowner-journey-map && npx tsc --noEmit` -- zero type errors
- `cd ~/Projects/homeowner-journey-map && npx vitest run` -- all existing tests pass
- Visual: Weekly paycheck is the first number visible on page load
- Visual: Scenario cards appear before chart and investment comparison
- Visual: No external links on page, no standalone net proceeds section
</verification>

<success_criteria>
- PAGE-01: Weekly paycheck hook visible immediately on page load in hero position
- PAGE-02: Sections reordered with scenarios before data/proof sections
- PAGE-04: "See a similar home" external link removed
- PAGE-06: Net proceeds removed from standalone section
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-future-scenarios-rework/07-01-SUMMARY.md`
</output>
